---
phase: 05-skill-output-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md
  - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md
  - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/wiki-optimizer/SKILL.md
autonomous: true
requirements: [OUT-01]
user_setup: []

must_haves:
  truths:
    - "Each of the three skills prints a final stdout line in the exact form `Output: <vault-relative-path>` after writing its file"
    - "braindump prints a vault-relative path (no C:\\ drive prefix) so the dashboard regex and openLinkText resolve it"
    - "humanizer writes its humanized output to a vault file (it previously only printed text inline) and prints that file's Output: line"
    - "wiki-optimizer prints the Output: line for its existing report path"
  artifacts:
    - path: "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md"
      provides: "Output: line contract for braindump (D-08)"
      contains: "Output: braindumps/"
    - path: "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md"
      provides: "Cowork-mode file write + Output: line contract for humanizer (D-07 prerequisite, Pitfall 6)"
      contains: "Output: braindumps/humanized-"
    - path: "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/wiki-optimizer/SKILL.md"
      provides: "Output: line contract for wiki-optimizer (D-08)"
      contains: "Output: wiki/optimizer-reports/"
  key_links:
    - from: "skill stdout final line"
      to: "dashboard parseOutputPath regex (05-03)"
      via: "structured prefix line"
      pattern: "^Output:\\s+(.+)$"
---

<objective>
Add a machine-parseable `Output: <vault-relative-path>` final stdout line to all three skills (braindump, humanizer, wiki-optimizer) so the dashboard can parse the output file path from stdout and render a clickable vault link. Additionally, give humanizer a Cowork-mode file-write step, because it currently produces its rewrite inline in chat and writes no file — without that, there is no path for the dashboard to link to.

This is the Wave 0 prerequisite for the dashboard's D-08 output-link parsing (implemented in 05-03). The regex the dashboard uses is `/^Output:\s+(.+)$/m`; these skill edits produce the line that regex matches.

Purpose: Close the skill side of the trigger->wait->result loop. Without these lines, `parseOutputPath(stdout)` returns null and the dashboard's success state shows "Done" with no link (graceful but incomplete), and the Phase 2 T4 re-run cannot assert a file was written.

Output: Three modified SKILL.md files, each ending its successful Cowork-mode run with an `Output:` line carrying a vault-relative path.

IMPORTANT — out-of-repo files: The three SKILL.md files live at `C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/` which is OUTSIDE this git worktree (`git ls-files` reports them outside the repository). Edit them in place on the filesystem. Do NOT attempt to `git add`/commit them from this repo — they are not tracked here. Only the .planning docs are committed in this repo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-skill-output-ux-polish/05-CONTEXT.md
@.planning/phases/05-skill-output-ux-polish/05-RESEARCH.md
@.planning/phases/05-skill-output-ux-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Output line to braindump SKILL.md</name>
  <files>C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md</files>
  <read_first>
    - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md — full file. Focus on "Step 5: Save or display" -> "Cowork mode" section (the block ending with `> File saved. [View braindump](computer://...)`). This is where the Output line is appended.
    - .planning/phases/05-skill-output-ux-polish/05-RESEARCH.md §"Skill Output Path Analysis (D-08)" — braindump row of the per-skill additions table; confirms format and vault-relative requirement.
    - .planning/phases/05-skill-output-ux-polish/05-PATTERNS.md §"Skills-dev/braindump/SKILL.md — MODIFY" — exact addition spec (lines 481-495).
  </read_first>
  <action>
    In the "Step 5: Save or display" -> "Cowork mode" subsection, AFTER the existing `> File saved. [View braindump](computer://...)` chat line, instruct the skill to also print one final stdout line in this exact form (per D-08): `Output: braindumps/YYYY-MM-DD_braindump.md` where the date matches the file just written. The path MUST be vault-relative (strip the `C:\Users\scull\OneDrive\ClaudeOS\` prefix from the absolute target path used in the same section) so the dashboard's openLinkText(path, '', 'tab') resolves it against vault root (Pitfall 5). Add a short instruction sentence such as: "After saving, also print a final line for tooling: `Output: braindumps/YYYY-MM-DD_braindump.md` (vault-relative path, no drive letter)." Do NOT change the existing absolute target-file path, the Mobile/standard-app branch, or the file-write logic. The Output line is additive and Cowork-mode only. The `Output:` literal prefix, the single space after the colon, and the vault-relative path are the load-bearing parts the dashboard regex `/^Output:\s+(.+)$/m` depends on (D-08).
  </action>
  <verify>
    <automated>No test harness in this project (package.json has no test script), so verify the file content directly. PowerShell (worktree shell):
    `Select-String -Path "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md" -Pattern '^Output: braindumps/' -Quiet` must return `True` (vault-relative Output line present), AND
    `Select-String -Path "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md" -Pattern '^Output: C:' -Quiet` must return `False` (no absolute/drive-letter Output line).
    Bash equivalent: `grep -E "^Output: braindumps/" "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md"` exits 0, and `grep -E "^Output: C:" "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md"` exits 1 (no match). Task passes only when the vault-relative line is found and the absolute form is absent.</automated>
  </verify>
  <acceptance_criteria>
    - braindump/SKILL.md Cowork-mode section contains a literal instruction line beginning `Output: braindumps/` with `_braindump.md` suffix.
    - The Output instruction uses a vault-relative path: the file does NOT contain the string `Output: C:` (no absolute/drive-letter Output line).
    - The pre-existing `> File saved. [View braindump]...` chat line is still present (not replaced).
    - The Mobile / standard Claude app mode branch is unchanged.
  </acceptance_criteria>
  <done>braindump prints `Output: braindumps/YYYY-MM-DD_braindump.md` as the final Cowork-mode stdout line; absolute-path Output forms are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add file-write step and Output line to humanizer SKILL.md</name>
  <files>C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md</files>
  <read_first>
    - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md — full file. Focus on the "## Process" section (steps 1-9) and "## Output Format" section. humanizer currently presents its rewrite inline in chat and writes NO file (RESEARCH §Pitfall 6 confirms).
    - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/braindump/SKILL.md "Step 5: Save or display" -> "Cowork mode" — analog for how to detect Cowork mode (file tools + Bash available) and write a vault file, then print the Output line. Mirror this environment-detection pattern.
    - .planning/phases/05-skill-output-ux-polish/05-RESEARCH.md §"Skill Output Path Analysis" humanizer row + §"Open Questions (RESOLVED)" Q1 (file location resolved to the researcher default `braindumps/humanized-YYYY-MM-DD-HHmmss.md`).
    - .planning/phases/05-skill-output-ux-polish/05-PATTERNS.md §"Skills-dev/humanizer/SKILL.md — MODIFY" (lines 499-509).
  </read_first>
  <action>
    Add a Cowork-mode output step to humanizer (it has none today). Mirror braindump's environment detection: when file tools and Bash are available (Cowork mode), after producing the FINAL humanized rewrite, write that final rewrite to a vault file at `braindumps/humanized-YYYY-MM-DD-HHmmss.md` (vault-relative; absolute target `C:\Users\scull\OneDrive\ClaudeOS\braindumps\humanized-YYYY-MM-DD-HHmmss.md`). Use the HHmmss timestamp to avoid collisions across multiple same-day runs. Then print one final stdout line in the exact form: `Output: braindumps/humanized-YYYY-MM-DD-HHmmss.md` (vault-relative, no drive letter — Pitfall 5). In Mobile/standard-app mode (no file tools), keep the existing inline-only behavior and do NOT print an Output line. Preserve the entire existing humanizing logic, the 29 pattern sections, the audit prompts, and the Output Format section — this task ADDS a file-write + Output step, it does not alter the rewriting behavior. Default folder `braindumps/` and filename `humanized-YYYY-MM-DD-HHmmss.md` per RESEARCH Open Question 1 (RESOLVED to the researcher default; treat as confirm-at-execution per Task note below). The `Output:` literal prefix + single space + vault-relative path are load-bearing for the dashboard regex `/^Output:\s+(.+)$/m` (D-07/D-08).

    CONFIRM-AT-EXECUTION: The humanizer output folder/filename (`braindumps/humanized-YYYY-MM-DD-HHmmss.md`) is the researcher default adopted in RESEARCH Open Question 1 (RESOLVED). If the executing session can ask the user, confirm the path before writing; otherwise use this default and record the choice in the SUMMARY.
  </action>
  <verify>
    <automated>No test harness, so verify the file content directly. PowerShell (worktree shell):
    `Select-String -Path "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md" -Pattern 'braindumps/humanized-' -Quiet` must return `True` (Cowork-mode file-write target present), AND
    `Select-String -Path "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md" -Pattern '^Output: braindumps/humanized-' -Quiet` must return `True` (vault-relative Output line present), AND
    `Select-String -Path "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md" -Pattern '^Output: C:' -Quiet` must return `False` (no absolute Output line).
    Bash equivalent: `grep -E "^Output: braindumps/humanized-" "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md"` exits 0, and `grep -E "^Output: C:" "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/humanizer/SKILL.md"` exits 1. Task passes only when the file-write target and the vault-relative Output line are both found and the absolute form is absent.</automated>
  </verify>
  <acceptance_criteria>
    - humanizer/SKILL.md contains a Cowork-mode instruction to WRITE the final rewrite to a vault file under `braindumps/humanized-...md` (the skill previously wrote no file).
    - humanizer/SKILL.md contains a literal `Output: braindumps/humanized-` instruction line with `.md` suffix.
    - The file does NOT contain `Output: C:` (no absolute Output line).
    - The Mobile/standard-app inline-only branch remains and prints no Output line.
    - All 29 existing pattern sections and the audit prompts are still present (rewriting behavior unchanged).
  </acceptance_criteria>
  <done>humanizer writes its final rewrite to `braindumps/humanized-YYYY-MM-DD-HHmmss.md` in Cowork mode and prints the matching vault-relative `Output:` line; inline-only mode is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add Output line to wiki-optimizer SKILL.md</name>
  <files>C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/wiki-optimizer/SKILL.md</files>
  <read_first>
    - C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/wiki-optimizer/SKILL.md — full file. Focus on "### Phase 2 — Assemble Report" (writes `wiki/optimizer-reports/OPTIMIZER-YYYY-MM-DD.md`) and "### Phase 3 — Log and Present" (presents summary + report link). The Output line goes at the end of Phase 3 presentation.
    - .planning/phases/05-skill-output-ux-polish/05-RESEARCH.md §"Skill Output Path Analysis" wiki-optimizer row — confirms the report path already exists; only the Output line is missing.
    - .planning/phases/05-skill-output-ux-polish/05-PATTERNS.md §"Skills-dev/wiki-optimizer/SKILL.md — MODIFY" (lines 513-522).
  </read_first>
  <action>
    In "Phase 3 — Log and Present", after step 2 (present the brief summary with a link to the report file), instruct the skill to print one final stdout line in the exact form: `Output: wiki/optimizer-reports/OPTIMIZER-YYYY-MM-DD.md` matching the report file already written in Phase 2. This path is already vault-relative (the wiki lives at `wiki/` relative to project/vault root), so no drive-letter stripping is needed. This is a one-line additive instruction only — do NOT change Phase 0-4 logic, the report template, the log-line format, or the approval prompt. The `Output:` literal prefix + single space + path are load-bearing for the dashboard regex `/^Output:\s+(.+)$/m` (D-08). wiki-optimizer is the self-contained skill that the dashboard runs via execFile (05-03); its stdout must carry the Output line for the link to appear.
  </action>
  <verify>
    <automated>No test harness, so verify the file content directly. PowerShell (worktree shell):
    `Select-String -Path "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/wiki-optimizer/SKILL.md" -Pattern '^Output: wiki/optimizer-reports/OPTIMIZER-' -Quiet` must return `True`.
    Bash equivalent: `grep -E "^Output: wiki/optimizer-reports/OPTIMIZER-" "C:/Users/scull/OneDrive/ClaudeOS/Skills-dev/wiki-optimizer/SKILL.md"` exits 0. Task passes only when the vault-relative Output line is found.</automated>
  </verify>
  <acceptance_criteria>
    - wiki-optimizer/SKILL.md Phase 3 section contains a literal `Output: wiki/optimizer-reports/OPTIMIZER-` instruction line ending `.md`.
    - The report-write logic in Phase 2 and the log-line format in Phase 3 step 1 are unchanged.
    - The approval prompt in Phase 3 step 3 is unchanged.
  </acceptance_criteria>
  <done>wiki-optimizer prints `Output: wiki/optimizer-reports/OPTIMIZER-YYYY-MM-DD.md` as its final stdout line after presenting results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill stdout -> dashboard parser | The `Output:` line is data the dashboard parses (05-03) and passes to Obsidian openLinkText. The skill controls this string. |
| skill file write -> vault filesystem | humanizer now writes a file into the vault (`braindumps/`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `Output:` line path | mitigate | Skills emit vault-relative paths only (no `C:\` drive prefix). Acceptance criteria assert the absence of `Output: C:` in each file. Vault-relative paths keep openLinkText scoped to the vault, avoiding absolute-path resolution surprises (Pitfall 5). |
| T-05-02 | Tampering | parsed output path -> openLinkText | accept (mitigated downstream in 05-03) | The parsed path is consumed only by Obsidian's openLinkText API (no filesystem write, no shell). 05-03 adds a `..` traversal guard before calling openLinkText. At the skill layer the path is author-controlled (the skill writes the file it names), so risk is LOW here. |
| T-05-03 | Tampering | humanizer vault file write | accept | humanizer writes to a fixed `braindumps/humanized-<timestamp>.md` name derived from the date, not from untrusted external input. No path component is user-controlled at write time. LOW risk. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (zero new dependencies — RESEARCH §Standard Stack). |

No HIGH-severity threats in this plan. All paths are author-controlled and vault-relative; the only cross-boundary data (the Output line) is hardened by the vault-relative acceptance check here and the traversal guard in 05-03.
</threat_model>

<verification>
- All three SKILL.md files contain a literal `Output: ` instruction line with a vault-relative path.
- No SKILL.md contains an absolute (`Output: C:`) Output line.
- humanizer additionally contains a Cowork-mode file-write instruction under `braindumps/humanized-`.
- Existing skill behavior (rewriting, report generation, braindump organizing) is preserved — only additive Output/file-write instructions were added.
</verification>

<success_criteria>
- braindump, humanizer, and wiki-optimizer each emit `Output: <vault-relative-path>` as a final stdout line on a successful Cowork-mode run.
- humanizer writes a vault file (previously it wrote none), enabling the dashboard output link (D-07) and the Phase 2 T4 re-run file assertion (05-04).
- The dashboard regex `/^Output:\s+(.+)$/m` (added in 05-03) will match each skill's final line.
</success_criteria>

<output>
Create `.planning/phases/05-skill-output-ux-polish/05-01-SUMMARY.md` when done. In the SUMMARY, record: the exact Output line format added to each skill, the humanizer output folder/filename chosen (and whether it was user-confirmed or the researcher default), and an explicit note that the three SKILL.md files are out-of-repo (not committed in this worktree).
</output>
