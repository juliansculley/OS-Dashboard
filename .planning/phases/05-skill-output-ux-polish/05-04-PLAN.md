---
phase: 05-skill-output-ux-polish
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - .planning/phases/02-dashboard-features/02-UAT.md
autonomous: false
requirements: [OUT-01]
user_setup: []

must_haves:
  truths:
    - "Phase 2 T4 is re-run end-to-end: an input-required skill is clicked, input is supplied, the button reaches Done, and the produced output file is confirmed to exist on disk in the correct directory"
    - "The skill that previously produced no file (T4 partial) now produces a verifiable file, closing the Phase 2 T4 gap"
    - "02-UAT.md T4 result is updated from `partial` to `pass` with a note recording the verified output file path"
  artifacts:
    - path: ".planning/phases/02-dashboard-features/02-UAT.md"
      provides: "Updated T4 result reflecting the closed gap"
      contains: "result: pass"
  key_links:
    - from: "input-required skill run (05-03)"
      to: "output file on disk"
      via: "skill writes file + dashboard parses Output: line"
      pattern: "braindumps/"
---

<objective>
Close the Phase 2 T4 gap by re-running the test end-to-end now that Phase 5 supplies skill input and verifies file output. Phase 2 T4 passed only on the visual state machine (spinner -> Done) but never asserted a file was written; the root cause was that skills like braindump/humanizer receive no input and produce nothing. Phase 5 (05-01 file output + Output line, 05-03 input panel + spawn stdin + output-link parsing) now makes a real output file possible.

This plan: (1) prepares any fixtures/setup automatically so the human verifier does not have to, (2) re-runs T4 end-to-end as a blocking human-verify checkpoint, and (3) updates 02-UAT.md to mark T4 `pass` with the verified output path recorded.

This plan is `autonomous: false` (one blocking human-verify checkpoint). It depends on all three prior plans being complete and the build being green.

Output: An updated 02-UAT.md (T4 partial -> pass) and a recorded verified output file path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-features/02-UAT.md
@.planning/phases/05-skill-output-ux-polish/05-01-SUMMARY.md
@.planning/phases/05-skill-output-ux-polish/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prepare T4 re-run fixtures and confirm build is green</name>
  <files>(no source changes — preparation + verification only)</files>
  <read_first>
    - .planning/phases/02-dashboard-features/02-UAT.md — the T4 test (test 4) and the Gaps section, so the re-run criteria match the original test intent plus the new file-output assertion.
    - .planning/phases/05-skill-output-ux-polish/05-01-SUMMARY.md — the exact output folder/filename each skill writes (e.g. braindump `braindumps/YYYY-MM-DD_braindump.md`, humanizer `braindumps/humanized-...md`) so Task 2 knows where to look on disk.
    - .planning/phases/05-skill-output-ux-polish/05-03-SUMMARY.md — confirmation the dashboard parses the Output line and the human-verify outcome.
  </read_first>
  <action>
    Before presenting the human checkpoint, do the setup so the verifier does nothing manual (per the project rule to prepare fixtures automatically):
    1. Confirm the build is current: run `npm run build` from the worktree root (run `npm install --strict-ssl=false` first only if node_modules is absent). It must exit 0 and update main.js.
    2. Confirm the target output directory exists so the skill can write to it: ensure `C:/Users/scull/OneDrive/ClaudeOS/braindumps/` exists (create it if missing — it is the braindump/humanizer output folder per 05-01). Do NOT pre-create the output note itself; the skill must produce it.
    3. Record a baseline: list the existing files in `C:/Users/scull/OneDrive/ClaudeOS/braindumps/` and note the current newest file/timestamp, so Task 2 can confirm a NEW file appeared after the run (distinguishes a fresh write from a pre-existing file).
    4. In the SUMMARY-prep notes for the checkpoint, state the exact expected output path the verifier should check on disk (from 05-01-SUMMARY), so step 2's verification is unambiguous.
    Do not change any source files in this task.
  </action>
  <verify>
    <automated>npm run build exits 0 (main.js present/updated at repo root). The braindumps output directory exists (creation succeeds or it was already present).</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with main.js updated.
    - The output directory `C:/Users/scull/OneDrive/ClaudeOS/braindumps/` exists.
    - A baseline list of pre-existing braindumps files (and the newest timestamp) is recorded for the post-run comparison.
    - The exact expected output path for the chosen test skill is stated for the checkpoint.
  </acceptance_criteria>
  <done>Build is green, the output directory exists, and a pre-run file baseline plus the expected output path are recorded — the verifier has zero manual setup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run Phase 2 T4 end-to-end with file-output assertion</name>
  <what-built>
    Phase 5 makes input-required skills produce real, verifiable output: the input panel (05-03) supplies stdin, the spawn execution runs `claude -p <skill>`, the skill writes a vault file and prints its `Output:` line (05-01), and the dashboard shows a clickable Open output link. Claude has confirmed the build is green and prepared the output directory and a pre-run file baseline (Task 1). This checkpoint re-runs the Phase 2 T4 test that previously passed only visually, this time asserting a file was actually written.
  </what-built>
  <how-to-verify>
    Reload the plugin in Obsidian and open the ClaudeOS Dashboard.

    1. On the Home page, click Braindump. The input panel opens. Type or paste a short block of notes (a few lines). Click `Run`.
    2. Watch the state machine: the button shows a spinner (loading), then transitions to `Done` (green). This reproduces the original T4 visual pass.
    3. NEW assertion (the gap T4 missed): confirm a file was actually written. Either click the `Open output` link (it should open a NEW braindump note in an Obsidian tab) OR open the folder `C:/Users/scull/OneDrive/ClaudeOS/braindumps/` and confirm a NEW file appeared (newer than the baseline Claude recorded in Task 1), named like `YYYY-MM-DD_braindump.md`, containing your input.
    4. Confirm the file is in the CORRECT directory (`braindumps/`) and its contents reflect the input you provided (not empty, not a placeholder).
    5. Optional cross-check: repeat once with Wiki Optimizer (self-contained, no input) and confirm its report file appears under `wiki/optimizer-reports/` and its Open output link opens it.

    The test PASSES only if both the visual state machine AND a real, correctly-located, non-empty output file are confirmed.
  </how-to-verify>
  <resume-signal>Type "approved" with the verified output file path (e.g. "approved — braindumps/2026-06-06_braindump.md exists and opened"), or describe the failure (e.g. "Done showed but no file was written", "file written to wrong directory", "Open output link opened nothing").</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update 02-UAT.md T4 result to pass</name>
  <files>.planning/phases/02-dashboard-features/02-UAT.md</files>
  <read_first>
    - .planning/phases/02-dashboard-features/02-UAT.md — full file. Update test 4's `result:` and `note:`, and the Summary counts and Gaps section to reflect the closed gap. Do this ONLY after the Task 2 checkpoint returns "approved".
  </read_first>
  <action>
    After the human verifier approves Task 2, update 02-UAT.md:
    1. Test 4 ("Skill Button State Machine"): change `result: partial` to `result: pass`. Replace the `note:` with a short line recording that Phase 5 closed the gap: input is now supplied via the inline panel (spawn stdin), the skill wrote a verified output file, and the dashboard showed the Open output link. Include the exact verified output path the verifier reported.
    2. Summary section: update the counts — `passed` +1, `partial` -> 0, `issues` -> 0 (if T4 was the only issue). Keep the total at 8.
    3. Gaps section: mark the "T4 — Skill output not verified" gap as RESOLVED in Phase 5, with a one-line pointer to this plan (05-04) and the verified output path. Do not delete the historical gap description; annotate it as resolved.
    If the verifier reported a failure instead of approval, do NOT mark pass — record the failure detail under Gaps and stop (the phase needs gap-closure planning).
  </action>
  <verify>
    <automated>MISSING — doc edit, no test harness. Verify by re-reading 02-UAT.md: test 4 shows `result: pass`, the Summary `partial` count is 0, and the Gaps T4 entry is annotated RESOLVED with the output path.</automated>
  </verify>
  <acceptance_criteria>
    - 02-UAT.md test 4 shows `result: pass` with a note citing the verified output file path.
    - The Summary section reflects 0 partial / 0 issues (T4 no longer partial).
    - The Gaps "T4 — Skill output not verified" entry is annotated as RESOLVED in Phase 5 (05-04), historical text retained.
    - The update was made only after a human "approved" signal (not on a reported failure).
  </acceptance_criteria>
  <done>02-UAT.md records T4 as pass with the verified output path, the summary counts are corrected, and the T4 gap is annotated resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification run -> vault filesystem | The T4 re-run causes a skill to write a real file into the vault. This exercises (does not introduce) the 05-01/05-03 boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering | T4 re-run skill execution | accept (mitigated in 05-01/05-03) | This plan only EXERCISES the execution paths hardened in 05-03 (SEC-03 guard order, stdin-only data, traversal guard) and 05-01 (vault-relative Output lines). It introduces no new execution surface. The test input is operator-supplied during a manual checkpoint. LOW residual risk. |
| T-05-13 | Information Disclosure | verified output path recorded in 02-UAT.md | accept | The recorded path is a vault-relative filename (e.g. `braindumps/...md`); no secret or credential is written to the UAT doc. LOW risk. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

No HIGH-severity threats. This is a verification/documentation plan that exercises already-mitigated paths.
</threat_model>

<verification>
- `npm run build` exits 0 before the checkpoint (Task 1).
- The output directory exists and a pre-run baseline is recorded (Task 1).
- Human verifier confirms BOTH the visual state machine AND a real, correctly-located, non-empty output file (Task 2).
- 02-UAT.md test 4 is updated to `pass` with the verified path; summary and gaps reflect the closure (Task 3).
</verification>

<success_criteria>
- Phase 2 T4 passes end-to-end with file-output verification — the gap that left it `partial` is closed.
- An input-required skill, given input through the new panel, produces a verifiable output file in the correct directory, and the dashboard's Open output link opens it.
- 02-UAT.md reflects the closed gap (T4 pass, 0 partial, T4 gap annotated resolved).
</success_criteria>

<output>
Create `.planning/phases/05-skill-output-ux-polish/05-04-SUMMARY.md` when done. Record: the verified output file path, the checkpoint outcome, and the 02-UAT.md edits made (or, if the checkpoint failed, the failure detail and a recommendation to run gap-closure planning).
</output>
