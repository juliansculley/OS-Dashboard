---
phase: 07-workouts-dashboard
plan: 06
type: execute
wave: 5
depends_on: ["07-04", "07-05"]
files_modified:
  - src/components/pages/WorkoutsPage.tsx
  - src/components/App.tsx
  - src/components/ui/Sidebar.tsx
autonomous: false
requirements: [WORKOUT-01, WORKOUT-02, WORKOUT-03, WORKOUT-04, WORKOUT-05]
must_haves:
  truths:
    - "A Workouts nav item (dumbbell icon) appears in the sidebar and opens the Workouts page"
    - "WorkoutsPage loads all four snapshots on mount and re-reads them on refreshNonce change"
    - "The tab bar switches between Muscle Volume / Progression / History / Context with no API call"
    - "Each tab receives loaded snapshot data; null/error/stale states render the correct EmptyState/Stale copy"
    - "npm run deploy succeeds and the page renders in Obsidian (verified by human)"
  artifacts:
    - path: "src/components/pages/WorkoutsPage.tsx"
      provides: "Page shell: refresh bar, tab bar, snapshot loading, tab routing, null/error/stale handling"
      exports: ["WorkoutsPage"]
    - path: "src/components/App.tsx"
      provides: "workouts entry in PAGES map"
      contains: "WorkoutsPage"
    - path: "src/components/ui/Sidebar.tsx"
      provides: "Workouts nav item"
      contains: "dumbbell"
  key_links:
    - from: "WorkoutsPage"
      to: "four snapshot files"
      via: "readJsonFile in useEffect on refreshNonce"
      pattern: "readJsonFile|refreshNonce"
    - from: "App.tsx PAGES"
      to: "WorkoutsPage"
      via: "PAGES.workouts = WorkoutsPage"
      pattern: "workouts:\\s*WorkoutsPage"
    - from: "Sidebar NAV_ITEMS"
      to: "workouts page"
      via: "dumbbell nav item dispatches setActivePage('workouts')"
      pattern: "dumbbell"
---

<objective>
Assemble the Workouts page: build `WorkoutsPage.tsx` (the shell that loads all four snapshots, owns tab state, renders the refresh bar + tab bar, routes to the four tab components from 07-04/05, and handles null/error/stale states), register it in `App.tsx`, add the `dumbbell` nav item to `Sidebar.tsx`, then deploy and UAT. This is the wave that lights up the whole feature.

Purpose: Connects WORKOUT-01..05 into a navigable page. Per the NOTION-04 pattern, snapshots load once on mount and re-read on `refreshNonce` change after a successful refresh.

Output: a working Workouts page in Obsidian, deployed and human-verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-workouts-dashboard/07-UI-SPEC.md
@.planning/phases/07-workouts-dashboard/07-RESEARCH.md
@src/components/pages/ProjectsPage.tsx
@src/components/App.tsx
@src/components/ui/Sidebar.tsx
@src/context/AppContext.tsx
@src/utils/readJsonFile.ts
@src/types.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WorkoutsPage shell — snapshot loading, tab bar, routing, states</name>
  <files>src/components/pages/WorkoutsPage.tsx</files>
  <read_first>
    - src/components/pages/ProjectsPage.tsx (SnapshotState<T> union + isSnapshotData guard to copy verbatim; useEffect-on-refreshNonce snapshot read pattern; inline EmptyState; stale label using isStale/formatHHmm)
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (Layout: page-level layout, tab bar, tab content area; Surface Inventory §1 WorkoutsPage; Interaction Contract → Tab switching + Refresh button; Copywriting Contract: page title "Workouts", tab labels + icons, first-run empty copy)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (Pattern 9 SnapshotState; snapshot read pattern code example)
    - src/components/ui/WorkoutsRefreshButton.tsx (from 07-04 — import and render in the refresh bar)
    - src/components/pages/workouts/MuscleVolumeTab.tsx, ProgressionTab.tsx (07-04), HistoryTab.tsx, ContextTab.tsx (07-05) — import and route to these by active tab
    - src/components/ui/RefreshButton.tsx (isStale, formatHHmm exports)
    - src/types.ts (the four snapshot interfaces)
  </read_first>
  <action>
Create `export function WorkoutsPage()`. Use `useAppContext()` for app/plugin/refreshNonce. Copy the `SnapshotState<T> = T | null | 'error'` union and `isSnapshotData` guard verbatim from ProjectsPage, plus the inline `EmptyState` component.

State: `activeTab: 'muscle-volume' | 'progression' | 'history' | 'context'` (default 'muscle-volume'); four snapshot states `muscleVolume`, `exercises`, `sessions`, `meta` each `SnapshotState<T>` of the matching interface.

Snapshot loading (NOTION-04 / research snapshot read pattern): four useEffects, one per snapshot, each keyed on `[plugin.settings.<path>, refreshNonce]`. Empty path → setState(null); else `readJsonFile<T>(app, path).then(d => setState(d !== null ? d : 'error'))`. Paths: workoutsMuscleVolumePath, workoutsExercisesPath, workoutsSessionsPath, workoutsMetaPath.

Layout (UI-SPEC Layout): page wrapper `.claudeos-page.claudeos-page--workouts`; `<h1 class="claudeos-page-title">Workouts</h1>`; a refresh bar containing `<WorkoutsRefreshButton />`; a stale banner using isStale/formatHHmm over the available generated_at timestamps (mirror ProjectsPage's stale logic — show "Stale — last synced {HH:mm}" but still render data); the tab bar `.claudeos-workouts-tabs` with four `.claudeos-workouts-tab` buttons (Muscle Volume/Progression/History/Context, each with its Lucide icon bar-chart-2 / trending-up / list / info via setIcon at 14px, uppercase label), `.active` on the current tab; and `.claudeos-workouts-tab-content` rendering the active tab.

Tab routing + state gating: pass loaded data to each tab. For each tab, if its required snapshot is `null` render the section's first-run/no-data EmptyState (exact copy from the Copywriting Contract Empty states table), if `'error'` render the error EmptyState, if loading-in-progress (still the initial `null` before first read resolves — use a separate "loading" sentinel or accept null→no-data) handle per the UI-SPEC. Only when `isSnapshotData(...)` is true mount the tab component with the snapshot prop (MuscleVolumeTab needs muscleVolume + meta; ProgressionTab needs exercises + meta; HistoryTab needs sessions; ContextTab needs meta). Tab switch is instant React state, no re-fetch, no animation (Interaction Contract). Tab state resets to 'muscle-volume' on navigation away/back (local state — not persisted).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Four useEffects read the four snapshot paths keyed on refreshNonce (grep refreshNonce + readJsonFile)
    - SnapshotState/isSnapshotData copied from ProjectsPage; null→no-data, 'error'→error EmptyState
    - Tab bar renders four tabs with the correct Lucide icons (bar-chart-2, trending-up, list, info) and labels
    - Imports and routes to all four tab components + WorkoutsRefreshButton
    - Page title "Workouts"; stale banner uses isStale/formatHHmm
  </acceptance_criteria>
  <done>WorkoutsPage loads all four snapshots, renders the refresh bar + tab bar, routes to the four tabs with correct state gating, and resets tab state on navigation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register page in App.tsx + add Sidebar nav item</name>
  <files>src/components/App.tsx, src/components/ui/Sidebar.tsx</files>
  <read_first>
    - src/components/App.tsx (PAGES Partial<Record<PageId,...>> map + ?? HomePage fallback — add the workouts entry and import)
    - src/components/ui/Sidebar.tsx (NAV_ITEMS array — add the workouts entry)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (Pattern 5 PageId extension, Pattern 6 Sidebar nav item)
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (Copywriting Contract: nav label "Workouts", icon dumbbell)
  </read_first>
  <action>
In `src/components/App.tsx`: import `WorkoutsPage` from `./pages/WorkoutsPage` and add `workouts: WorkoutsPage,` to the `PAGES` map. (The map is `Partial<Record<PageId, ...>>` so this is type-safe; the `?? HomePage` fallback is unaffected.)

In `src/components/ui/Sidebar.tsx`: append to `NAV_ITEMS`: `{ id: 'workouts', label: 'Workouts', iconId: 'dumbbell' }`. The `id` is valid because 07-03 extended the PageId union with 'workouts'.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - App.tsx imports WorkoutsPage and PAGES contains `workouts: WorkoutsPage`
    - Sidebar NAV_ITEMS contains the workouts entry with iconId 'dumbbell' and label 'Workouts'
  </acceptance_criteria>
  <done>The Workouts page is registered in the router and reachable from the sidebar dumbbell nav item.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deploy + UAT the Workouts page in Obsidian</name>
  <what-built>The complete Workouts page wired into the dashboard, ready to deploy and test in Obsidian.</what-built>
  <how-to-verify>
    Build + deploy first (per CLAUDE.md — use deploy, not build):
    1. Run `npm run deploy` from this worktree. If it fails (tsc or copy), stop and report the error before UAT.
    2. Reload Obsidian (Ctrl+R or close/reopen).

    Settings prerequisite:
    3. In plugin Settings → Workouts Data, confirm/set the five paths. Set `Workouts Sync Script` to the MAIN checkout path: `C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\scripts\notion-workouts-sync.mjs` (per CLAUDE.md Settings Paths — the script must point at main, which is testable only after merge; until then test the script from the CLI as in 07-01/02).

    UAT in Obsidian:
    4. Click the Workouts nav item (dumbbell) — the Workouts page opens with the four tabs.
    5. Muscle Volume tab: bar chart (total sets/muscle) and weekly-trend line render; toggle 7d/14d/28d/This Meso and Primary+Secondary/Primary only — charts update with no error. Confirm bars use distinct palette colors (not black/transparent — that would mean CSS-var resolution failed).
    6. Progression tab: pick an exercise — the line chart and the two PR tiles render; toggle legend datasets.
    7. History tab: sessions list reverse-chronologically; apply split/difficulty/results/mesocycle filters and confirm the list narrows; click a row opens the Notion session.
    8. Context tab: mesocycle card shows days remaining; bodyweight sparkline renders.
    9. Re-open the Workouts pane (switch away and back, or close/reopen the leaf) — confirm NO "Canvas is already in use" error in the console (research Pitfall 1).
    10. Empty-state spot check: temporarily blank one snapshot path in Settings → that tab shows the no-data EmptyState; restore the path.
  </how-to-verify>
  <acceptance_criteria>
    - `npm run deploy` succeeds (tsc clean + copy)
    - All four tabs render real data
    - Window/attribution/filter/legend interactions work with no console errors
    - No "Canvas is already in use" on pane re-open
    - Chart bars/lines show palette colors (CSS-var resolution works)
    - Notion row click-through works
  </acceptance_criteria>
  <resume-signal>Type "approved" when the Workouts page works in Obsidian, or describe any issue.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | snapshot data rendered in page | mitigate | All snapshot strings rendered as React text children (auto-escaped) or via sanitizeText for Chart.js labels; no dangerouslySetInnerHTML (SEC-01) |
| T-07-13 | Information Disclosure | session URLs | mitigate | URLs only in href with rel="noopener noreferrer" target="_blank" via ListRow (inherited Phase 3 pattern) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npm run deploy` succeeds
- Human UAT confirms all four tabs, interactions, no canvas error, palette colors
</verification>

<success_criteria>
WORKOUT-01..05 are deliverable end-to-end: a navigable Workouts page with all four tabs rendering snapshot data, interactive client-side controls, a working refresh button, and no canvas lifecycle errors — verified in Obsidian.
</success_criteria>

<artifacts_this_phase_produces>
- `src/components/pages/WorkoutsPage.tsx` (new)
- `src/components/App.tsx` (modified — PAGES.workouts)
- `src/components/ui/Sidebar.tsx` (modified — dumbbell nav item)
- Deployed plugin (main.js + styles.css copied to the Obsidian plugin dir)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-workouts-dashboard/07-06-SUMMARY.md` when done
</output>
