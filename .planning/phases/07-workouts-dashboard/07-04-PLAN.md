---
phase: 07-workouts-dashboard
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - src/components/ui/WorkoutsRefreshButton.tsx
  - src/components/pages/workouts/MuscleVolumeTab.tsx
  - src/components/pages/workouts/ProgressionTab.tsx
autonomous: true
requirements: [WORKOUT-01, WORKOUT-02, WORKOUT-05]
must_haves:
  truths:
    - "WorkoutsRefreshButton runs notion-workouts-sync.mjs via execFile and bumps refreshNonce on success"
    - "MuscleVolumeTab renders a horizontal bar chart (total sets/muscle) and a weekly-trend line chart from snapshot data"
    - "Window presets (7/14/28/This Meso) and the attribution toggle re-aggregate client-side with no API call"
    - "ProgressionTab renders an exercise-selectable multi-dataset line chart and two PR tiles"
    - "Every Chart instance destroys the prior instance before create and in cleanup (no 'Canvas is already in use')"
    - "All Notion-sourced muscle/exercise labels pass through sanitizeText before reaching Chart.js"
  artifacts:
    - path: "src/components/ui/WorkoutsRefreshButton.tsx"
      provides: "Refresh button cloned from RefreshButton, pointing at workoutsSyncScriptPath + workoutsMetaPath"
      exports: ["WorkoutsRefreshButton"]
    - path: "src/components/pages/workouts/MuscleVolumeTab.tsx"
      provides: "Bar + trend charts, window presets, attribution toggle"
      exports: ["MuscleVolumeTab"]
    - path: "src/components/pages/workouts/ProgressionTab.tsx"
      provides: "Exercise select, progression line chart, PR tiles"
      exports: ["ProgressionTab"]
  key_links:
    - from: "MuscleVolumeTab"
      to: "Chart.js canvas"
      via: "useEffect with destroy-before-create + sanitizeText labels"
      pattern: "chartRef.*destroy|sanitizeText"
    - from: "WorkoutsRefreshButton"
      to: "notion-workouts-sync.mjs"
      via: "execFile(node, [workoutsSyncScriptPath])"
      pattern: "execFile"
---

<objective>
Build three leaf components for the Workouts page: `WorkoutsRefreshButton` (clone of RefreshButton targeting the workout sync script), `MuscleVolumeTab` (the headline view — bar chart of total sets/muscle, weekly-trend line chart, window presets, attribution toggle), and `ProgressionTab` (exercise selector, multi-dataset progression line chart, PR tiles). These are independently compilable components that receive snapshot data via props/context; the WorkoutsPage shell that wires them together is built in 07-06.

Purpose: WORKOUT-01 (muscle volume) and WORKOUT-02 (progression) render here; WORKOUT-05's button variant runs the workout sync. Per D7, all window/attribution math is client-side over the loaded snapshot (zero API calls).

Output: three new component files implementing the UI-SPEC §4, §5, and §2 contracts with the verified Chart.js canvas lifecycle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-workouts-dashboard/07-UI-SPEC.md
@.planning/phases/07-workouts-dashboard/07-RESEARCH.md
@C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md
@src/components/ui/RefreshButton.tsx
@src/context/AppContext.tsx
@src/types.ts
@src/utils/sanitizeText.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WorkoutsRefreshButton + Chart.js registration module</name>
  <files>src/components/ui/WorkoutsRefreshButton.tsx</files>
  <read_first>
    - src/components/ui/RefreshButton.tsx (clone the full state machine, IconSlot, spinner, refreshLastSynced, handleClick guard, execFile call — substitute workout settings keys)
    - src/context/AppContext.tsx (useAppContext gives app, plugin, triggerRefresh)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (Pattern 2: tree-shakeable Chart.register import list; Pattern 8: script path + last-synced source)
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (Chart.js Integration Notes; Refresh button state machine copy: Refresh/Synced/Failed/Last synced HH:mm)
  </read_first>
  <action>
Clone `RefreshButton.tsx` verbatim into `src/components/ui/WorkoutsRefreshButton.tsx` as `export function WorkoutsRefreshButton()`, changing only: read the script path from `plugin.settings.workoutsSyncScriptPath` (not syncScriptPath); read last-synced from `plugin.settings.workoutsMetaPath` (not tasksSnapshotPath). Keep the idle/loading/success/error state machine, IconSlot, spinner, the blank-path guard (set error immediately, no execFile), `execFile(node, [script])` (no shell, SEC-02/SEC-03), and `triggerRefresh()` on success — all identical. Reuse the exported `isStale`/`formatHHmm` from RefreshButton.tsx (import them; do not redefine).

Add the Chart.js registration at MODULE SCOPE in this file (or a small shared `src/components/pages/workouts/chartSetup.ts` that all tab files import once — choose one; document the choice). Registration (research Pattern 2): `import { Chart, BarController, BarElement, LineController, LineElement, PointElement, CategoryScale, LinearScale, Tooltip, Legend, Filler } from 'chart.js'; Chart.register(BarController, BarElement, LineController, LineElement, PointElement, CategoryScale, LinearScale, Tooltip, Legend, Filler);` (Filler is needed for the bodyweight sparkline fill in 07-05 — include it now in the shared setup). Must run once at import, never inside a component. Do NOT use chart.js/auto and do NOT mutate global Chart.defaults (research Anti-Patterns).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - WorkoutsRefreshButton.tsx references workoutsSyncScriptPath and workoutsMetaPath (grep both)
    - execFile called with array args `[script]` — no shell string interpolation
    - Chart.register present at module scope with the selective import list (not chart.js/auto)
    - Blank-path guard present (error state without execFile when path empty)
  </acceptance_criteria>
  <done>WorkoutsRefreshButton runs the workout sync via execFile with the workout settings keys, and Chart.js is registered once at module scope with tree-shakeable imports.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MuscleVolumeTab — bar + trend charts, window presets, attribution toggle</name>
  <files>src/components/pages/workouts/MuscleVolumeTab.tsx</files>
  <read_first>
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (§4 MuscleVolumeTab: controls 4a, bar chart 4b, trend line 4c, empty states 4d; Muscle Group Display Order; Interaction Contract → Window preset buttons + Attribution toggle; Chart.js Integration Notes; Copywriting Contract chart labels/tooltips/empty states)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (Pattern 3 canvas lifecycle, Pattern 4 CSS var resolution, code examples: horizontal bar chart, useEffect cleanup)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§6.2 set-counting, §8 MuscleVolumeSnapshot shape)
    - src/types.ts (MuscleVolumeSnapshot, MuscleWeekCell, WorkoutsMetaSnapshot)
    - src/utils/sanitizeText.ts (sanitizeText for labels)
    - src/context/AppContext.tsx
  </read_first>
  <action>
Create `export function MuscleVolumeTab(props: { muscleVolume: MuscleVolumeSnapshot; meta: WorkoutsMetaSnapshot | null })` (the parent passes already-loaded snapshot data; null/error/loading states are handled by the parent in 07-06, but ALSO render the in-tab "no sessions in window" inline message here). Local state: `windowDays` (default 28) and `attributionMode` ('both' default | 'primary').

Controls (UI-SPEC 4a): a WINDOW segmented button group (7d/14d/28d/This Meso) and an ATTRIBUTION group (Primary + Secondary / Primary only), using the `.claudeos-workouts-preset-btn`/`.active` classes and group labels per the Copywriting Contract. `This Meso` is disabled (faint, pointer-events none) when `meta?.current_meso` is null; when enabled it computes the window from current_meso.start to today.

Client-side aggregation (D7, Interaction Contract): pick the source array by attributionMode — `weekly_with_secondary` for 'both', `weekly_primary_only` for 'primary'. Filter `MuscleWeekCell[]` to weeks within the selected window (compare the ISO week to a cutoff derived from windowDays). Group/sum by muscle for the bar chart; keep per-week series per muscle for the trend chart. Order muscles per the UI-SPEC Muscle Group Display Order (Chest, Back, Shoulders, Side Delt, Triceps, Biceps, Quadriceps, Hamstrings, Glutes, Lower Back, Traps, Abs, Calves; unknowns appended alphabetically); exclude any Cardio/knee defensively. Exclude muscles with zero sets in the window from both charts. If the window has zero sets total, render the inline `claudeos-list-empty` message "No sessions in this window. Try a wider range." instead of charts.

Bar chart (UI-SPEC 4b): horizontal bar (`type:'bar'`, `indexAxis:'y'`), canvas height 320px, one `--cos-chart-*` color per bar (resolve all 13 palette colors + `--cos-muted` via `getComputedStyle(canvasEl.ownerDocument.documentElement).getPropertyValue('--cos-chart-N').trim()` with hex fallbacks — research Pattern 4), labels sanitized via `sanitizeText` (SEC-01), `animation:false`, `responsive:true`, `maintainAspectRatio:false`, legend hidden, tooltip `{Muscle}: {N} sets`, x grid muted at ~50% alpha, y grid hidden, tick color muted. Section heading "Total Sets".

Trend line chart (UI-SPEC 4c): `type:'line'`, canvas height 200px, one dataset per muscle (matching bar colors), x = ISO week labels, `tension:0.3`, `pointRadius:3`, `fill:false`, legend shown above (clickable native toggle), multi-dataset tooltip sorted desc, `animation:false`. Section heading "Weekly Trend".

Canvas lifecycle (research Pattern 3 — CRITICAL): each chart uses `const canvasRef = useRef<HTMLCanvasElement>(null); const chartRef = useRef<Chart|null>(null);`. In the creation useEffect: guard on canvasRef.current, call `chartRef.current?.destroy()` BEFORE `new Chart(...)`, and return a cleanup that calls `chartRef.current?.destroy(); chartRef.current = null;`. Dependency array includes the aggregated data + windowDays + attributionMode so charts rebuild on control changes. Never set a fixed canvas `id`. This prevents "Canvas is already in use" on Obsidian pane re-open.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Both charts call `chartRef.current?.destroy()` before `new Chart` AND in a cleanup return (grep destroy count >= 4 across the two effects)
    - sanitizeText applied to muscle labels before Chart.js (grep sanitizeText)
    - Window presets and attribution toggle drive local state; aggregation reads weekly_primary_only vs weekly_with_secondary (grep both names)
    - getComputedStyle used to resolve --cos-chart-* (grep getPropertyValue('--cos-chart)
    - "This Meso" disabled when current_meso is null
    - Empty-window inline message uses exact copy "No sessions in this window. Try a wider range."
    - No chart.js/auto import; no Chart.defaults global mutation
  </acceptance_criteria>
  <done>MuscleVolumeTab renders the bar + trend charts with palette colors resolved at mount, supports window/attribution controls via client-side re-aggregation, sanitizes labels, and manages canvas lifecycle correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ProgressionTab — exercise select, progression line chart, PR tiles</name>
  <files>src/components/pages/workouts/ProgressionTab.tsx</files>
  <read_first>
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (§5 ProgressionTab: selector 5a, line chart 5b, PR tiles 5c, empty states 5d; Copywriting Contract: EXERCISE label, "Select an exercise...", Best Weight / Best 1RM Est, progression tooltip; dataset styles + visibility defaults)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (Pattern 3 canvas lifecycle, Pattern 4 CSS var resolution)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§6.4 per-exercise series, §8 ExercisesSnapshot/ExerciseSeries/ExercisePoint)
    - src/types.ts (ExercisesSnapshot, ExerciseSeries, ExercisePoint, WorkoutsMetaSnapshot)
    - src/utils/sanitizeText.ts
  </read_first>
  <action>
Create `export function ProgressionTab(props: { exercises: ExercisesSnapshot; meta: WorkoutsMetaSnapshot | null })`. Local state: `selectedExerciseId` (default '').

Exercise selector (UI-SPEC 5a): native `<select class="claudeos-workouts-exercise-select">` with an EXERCISE group label; a disabled selected placeholder option "Select an exercise..."; options = exercises with `points.length >= 2`, sorted alphabetically by name, option text sanitized via `sanitizeText` (defensive — these reach the DOM as option labels). On change set selectedExerciseId.

When no exercise selected: render inline `claudeos-list-empty` "Select an exercise to see progression." When selected exercise has 0-1 points: render EmptyState "Not enough data" / "This exercise has been logged fewer than 2 times. Log more sessions to see a trend." (copy the inline EmptyState component pattern from ProjectsPage.)

Progression line chart (UI-SPEC 5b): `type:'line'`, canvas height 280px. Datasets from the selected ExerciseSeries.points (sorted ascending by date; x labels MM-DD): Weight (solid, `--cos-chart-0`, pointRadius 4, shown by default), Volume (dashed [2,2], `--cos-chart-1`, shown by default), 1RM est (dotted [1,3], `--cos-chart-3`, hidden by default), and an optional Bodyweight overlay on a right y-axis (dashed [4,4], `--cos-chart-9`) only when `meta?.bodyweight` covers the exercise's date range. Left y-axis min 0 shared by Weight/Volume/1RM; right y-axis for bodyweight labelled "BW (lbs)". `spanGaps:true` for null points. Tooltip lists non-null dataset values per date. Legend shown above with names Weight/Volume/1RM est/Bodyweight (native click toggle). `animation:false`, `responsive:true`, `maintainAspectRatio:false`. Resolve colors via getComputedStyle (research Pattern 4). Canvas lifecycle: destroy-before-create + cleanup destroy (research Pattern 3), deps include selectedExerciseId.

PR tiles (UI-SPEC 5c): `.claudeos-pr-tiles` row with two `.claudeos-pr-tile`s — "Best Weight" (value = series.best_weight) and "Best 1RM Est" (value = series.best_1rm). Render em dash `—` when null (faint), per StatusTile pattern. Section heading "Personal Records".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Chart uses destroy-before-create + cleanup destroy (grep chartRef.*destroy)
    - getComputedStyle resolves --cos-chart-* colors
    - Selector only lists exercises with points.length >= 2
    - Empty states use exact copy strings from the UI-SPEC ("Select an exercise to see progression.", "Not enough data", "Best Weight", "Best 1RM Est")
    - 1RM dataset hidden by default; Weight + Volume shown by default
    - PR tile values fall back to em dash when null
  </acceptance_criteria>
  <done>ProgressionTab renders the exercise-selectable progression chart with correct dataset styling/visibility and two PR tiles, with the canvas lifecycle and empty states per the UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot strings → Chart.js labels | Notion muscle/exercise names reach the canvas (non-React surface) |
| settings path → execFile | workoutsSyncScriptPath used to spawn node |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering (XSS) | Chart.js labels / select options | mitigate | sanitizeText() on all Notion-sourced names before Chart.js labels and option text (SEC-01) |
| T-07-08 | Tampering (injection) | WorkoutsRefreshButton execFile | mitigate | execFile(node, [workoutsSyncScriptPath]) — no shell, no interpolation; blank-path guard fails fast (SEC-02/SEC-03) |
| T-07-09 | DoS (memory) | Chart canvas re-open | mitigate | destroy-before-create + cleanup destroy on every chart (research Pattern 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for all three components
- Canvas destroy pattern present on every chart
- sanitizeText applied to all Notion labels
</verification>

<success_criteria>
WORKOUT-01 muscle-volume view and WORKOUT-02 progression view render from snapshot data with interactive client-side controls; WORKOUT-05's workout refresh button exists. All charts manage the canvas lifecycle and sanitize labels.
</success_criteria>

<artifacts_this_phase_produces>
- `src/components/ui/WorkoutsRefreshButton.tsx` (new)
- `src/components/pages/workouts/MuscleVolumeTab.tsx` (new)
- `src/components/pages/workouts/ProgressionTab.tsx` (new)
- `src/components/pages/workouts/chartSetup.ts` (new, if the shared-registration option is chosen)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-workouts-dashboard/07-04-SUMMARY.md` when done
</output>
