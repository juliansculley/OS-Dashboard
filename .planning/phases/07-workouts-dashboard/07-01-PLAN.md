---
phase: 07-workouts-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/notion-workouts-sync.mjs
autonomous: false
requirements: [WORKOUT-01, WORKOUT-02, WORKOUT-03, WORKOUT-04]
user_setup:
  - service: notion
    why: "The sync script queries six Notion workout databases via REST; each must have the existing ClaudeOS integration shared or queries return 404."
    dashboard_config:
      - task: "Share the 'ClaudeOS Dashboard Sync' integration with all six workout databases (Workout Schedule, Workout Logbook, Exercises, Muscles Groups, Mesocycle timeline, JS measurements)"
        location: "Notion → open each database → ••• menu → Connections → add 'ClaudeOS Dashboard Sync'"

must_haves:
  truths:
    - "Running the script with the token set writes four JSON files into <repo>/.dashboard-data/"
    - "A window with 3 sets bench (Chest/Triceps) + 4 sets tricep extension (Triceps) yields Chest=3, Triceps=7 in the muscle-volume snapshot (worked-example acceptance test)"
    - "Each session row carries a parsed template (Name minus trailing MMDDYY) and tags[] (matched Push/Pull/Full/Arms/Hyp/Str tokens)"
    - "Each exercise series carries weight/volume/est_1rm points sorted ascending by date"
    - "The muscle-volume snapshot contains BOTH weekly_with_secondary and weekly_primary_only arrays"
  artifacts:
    - path: "scripts/notion-workouts-sync.mjs"
      provides: "Six-query Notion REST sync with in-memory relation joins, §6 aggregations, atomic snapshot writes"
      contains: "queryDataSource"
    - path: ".dashboard-data/workouts-muscle-volume.json"
      provides: "Per-week-per-muscle matrix (both attribution arrays), per-session breakdown, ordered muscle list"
    - path: ".dashboard-data/workouts-exercises.json"
      provides: "Per-exercise weight/volume/1RM series + PR tiles"
    - path: ".dashboard-data/workouts-sessions.json"
      provides: "Session list with template, tags, difficulty, results, meso, total_sets, url"
    - path: ".dashboard-data/workouts-meta.json"
      provides: "Current mesocycle + bodyweight series"
  key_links:
    - from: "scripts/notion-workouts-sync.mjs"
      to: "Notion REST /v1/data_sources/{id}/query"
      via: "queryDataSource() with Notion-Version 2025-09-03"
      pattern: "data_sources/.*query"
    - from: "Logbook line"
      to: "session date + exercise muscles"
      via: "in-memory join on Workout Log and Exercise relations"
      pattern: "Workout Log|Exercise"
---

<objective>
Build `scripts/notion-workouts-sync.mjs`, a standalone Node ESM script that pulls the six Notion workout databases via REST, joins their relations in memory, runs the §6 aggregation algorithms, and writes four atomic JSON snapshots into `<repo>/.dashboard-data/`. This is the headline, logic-heavy plan that de-risks the entire phase — the plugin is a pure display layer over these snapshots.

Purpose: WORKOUT-01/02/03/04 all depend on pre-computed snapshots. Per design decision D8, this is a SEPARATE script (sibling to `notion-sync.mjs`), run only on demand. Per D7, the script must emit enough granularity (per-week-per-muscle matrix, per-session breakdown, per-exercise series) for the plugin to re-window/filter/toggle entirely client-side.

Output: `notion-workouts-sync.mjs` + four snapshot files verified against the worked example (Chest 3, Triceps 7).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md
@.planning/phases/07-workouts-dashboard/07-RESEARCH.md
@scripts/notion-sync.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build notion-workouts-sync.mjs — queries, joins, aggregation, snapshots</name>
  <files>scripts/notion-workouts-sync.mjs</files>
  <read_first>
    - scripts/notion-sync.mjs (clone loadToken, API_BASE, NOTION_VERSION, queryDataSource paginated helper, notionUrl, writeAtomic, scriptDir/outputDir resolution, main() structure, Node 18+ fetch guard — reuse verbatim where possible)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§2 data source UUIDs, §3 verbatim property names, §4 data-quality rules, §6 aggregation algorithms, §8 snapshot contracts, §10 locked decisions)
  </read_first>
  <action>
Clone the structure of `notion-sync.mjs` exactly: the Node 18+ fetch guard, `loadToken()` (env var NOTION_TOKEN then `%USERPROFILE%\.claudeos\notion.env` fallback — never read inside the synced vault), `API_BASE` = 'https://api.notion.com/v1/data_sources/', `NOTION_VERSION` = '2025-09-03', the paginated `queryDataSource(token, uuid, body)` helper, `notionUrl(result)`, and `writeAtomic(finalPath, obj)` (temp + rename, stamps generated_at). Resolve outputDir as `join(scriptDir, '..', '.dashboard-data')` and mkdir recursive.

Define `DATA_SOURCES` with the six UUIDs from design §2: schedule `065624dc-9a59-45ce-9465-41023102df25`, logbook `9975dd7c-65af-4639-ae63-e35b2df159c1`, exercises `7bde7692-e1a6-44d3-8cf1-9420bd3da08a`, muscles `518d9626-0074-43a8-9b32-2e47474f31b3`, mesocycles `914a59ce-4069-44ca-b335-0cd325ca2f35`, measurements `636c8c04-8a87-490d-9574-cc2478a187fa`.

Run six queries: schedule (sort Date desc), logbook (all), exercises (all), muscles (all), mesocycles (all), measurements (all). Build lookup maps keyed by Notion page id for muscles (id→Name), exercises, and sessions.

Build the primary-muscle map per design §6.3: for each exercise, resolve `Prime muscle` relation ids to muscle names via the muscles map → that is `primary[]`. This run-time seeded primary map is authoritative. `secondary[]` is left empty in THIS plan (the enrichment file + fallback chain is wired in 07-02); leave a clearly marked extension point for it.

Join logbook lines per design §6.1: for each line resolve `Workout Log` relation → session Date (date-only) and session_id/name; resolve `Exercise` relation → exercise_id/name and primary/secondary muscles. Read `Sets`/`Reps`/`Weight` as plain numbers. Read `Volume` and `1RM` from REST formula values (`props.Volume.formula.number`, `props['1RM'].formula.number`); volume falls back to Sets×Reps×Weight when the formula value is null (D5). Inclusion rule (§4.5/6.1): count a line for set-counting only when `Sets > 0`; include in volume/weight series only when both Reps and Weight are non-null. Do NOT filter on `Done` (unreliable, §4.4).

Muscle-group set-counting per design §6.2: for each line, union primary ∪ secondary (dedup); add `line.Sets * weight_factor` to the muscle's bucket, where weight_factor = 1.0 for primary and SECONDARY_WEIGHT (constant = 1.0 per D2) for secondary-only muscles. Aggregate at three grains from the same pass: per-session (sets_by_muscle), per ISO week (`YYYY-Www`), per-window total. Keep `Side Delt` and `Shoulders` as separate buckets — no merging (D3). Exclude `Cardio` and `knee` from all muscle math (§6.3). Emit BOTH `weekly_with_secondary` (the default, secondary at 1.0) and `weekly_primary_only` (sum only primary-muscle contributions) as `MuscleWeekCell[]` arrays — this is the array swap the UI attribution toggle uses (research Pitfall 6).

Per-exercise series per design §6.4: group included lines by exercise_id, sort by date ascending, emit `{date, weight, volume, est_1rm}` points; compute best_weight and best_1rm; carry primary/secondary.

Session rows per design §6 + D6: parse `template` = Name with the trailing `MMDDYY` date token stripped (only the trailing 6-digit token is the date; a mid-string year like `25`/`26` stays in the template); `tags[]` = tokens from {Push, Pull, Full, Arms, Hyp, Str} present in Name. Read `Difficulty` (multi-select array), `WO results ` (note trailing space in property name) → results array, `Location`. Date-join the mesocycle (§6.5): the timeline row whose Date range contains the session Date → meso_name. total_sets = sum of line.Sets for that session. url = notionUrl(session).

Meta per design §6.5/§8: current_meso = the timeline row whose Date range contains today (name, wo_type from `WO Type`, focus from `Focus`, status, end = range end); bodyweight = measurements where `Measurement == "Weight"`, mapped to `{date, value: props.Number.number}` sorted ascending.

Shape the four output objects to design §8 contracts exactly (MuscleVolumeSnapshot, ExercisesSnapshot, SessionsSnapshot, WorkoutsMetaSnapshot — but note `weekly` is split into the two attribution arrays named above; include `muscles` ordered display list excluding Cardio/knee, `window_default_days` = 28, `secondary_weight` = 1.0). Write each with `writeAtomic` to workouts-muscle-volume.json, workouts-exercises.json, workouts-sessions.json, workouts-meta.json. Print a one-line success summary to stdout (sessions/lines/exercises/muscles counts) like notion-sync.mjs. Use ONLY Node built-ins (fs/promises, path, os, url) — no npm deps in the script.
  </action>
  <verify>
    <automated>node --check scripts/notion-workouts-sync.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/notion-workouts-sync.mjs` exits 0 (valid ESM syntax)
    - Grep confirms all six UUIDs present: `grep -c "065624dc\|9975dd7c\|7bde7692\|518d9626\|914a59ce\|636c8c04" scripts/notion-workouts-sync.mjs` returns 6
    - Grep confirms both attribution arrays emitted: file contains `weekly_with_secondary` and `weekly_primary_only`
    - Grep confirms `Notion-Version` header value `2025-09-03` and `data_sources/` path present
    - Grep confirms `WO results ` is read with the trailing space (the verbatim property name)
    - Script imports only from 'fs/promises', 'path', 'os', 'url' — no other import lines
  </acceptance_criteria>
  <done>The script parses, queries six data sources, joins relations in memory, runs §6 aggregations with secondary_weight 1.0 and Side Delt/Shoulders separate, emits both attribution arrays, and writes four atomic snapshots with generated_at.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live run + worked-example acceptance test</name>
  <what-built>The complete `notion-workouts-sync.mjs` ready for its first live run against Notion.</what-built>
  <how-to-verify>
    1. Confirm the ClaudeOS integration is shared with ALL SIX workout databases (see user_setup) — otherwise queries 404.
    2. Run from the command line (the Node path on this machine):
       `& "C:\Users\scull\AppData\Local\nvm\v24.12.0\node.exe" scripts\notion-workouts-sync.mjs`
    3. Confirm it prints a success summary and exits 0 (no 404s).
    4. Open `.dashboard-data\workouts-muscle-volume.json`. Find a window/session containing 3 sets bench press (Chest/Triceps) + 4 sets tricep extension (Triceps). Confirm the per-session breakdown reads Chest = 3, Triceps = 7 (design §11 acceptance test). If your real data has no such session, hand-check any one session's sets_by_muscle against the bench=primary+secondary rule.
    5. Confirm all four JSON files exist, each with a `generated_at` ISO timestamp.
    6. Confirm `workouts-meta.json` shows a plausible current_meso and a bodyweight array.
  </how-to-verify>
  <acceptance_criteria>
    - Script runs without 404 against all six databases
    - Worked-example check passes (Chest 3 / Triceps 7, or equivalent hand-check)
    - Four snapshot files exist with generated_at
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm the snapshots are correct, or describe any aggregation mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notion REST API → script | Untrusted strings (names, notes) enter the script and are written to snapshots |
| script → token source | Token must come from outside the synced vault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | NOTION_TOKEN handling | mitigate | Clone `loadToken()` — read env var or `%USERPROFILE%\.claudeos\notion.env` only; token never written to any snapshot or vault file (NOTION-02) |
| T-07-02 | Tampering | Notion-sourced strings in snapshots | accept (defer) | Strings are stored as plain JSON here; sanitization happens at render time in the plugin (07-03 util, 07-04/05/06 usage) per SEC-01 |
| T-07-03 | Denial of Service | Pagination loop | mitigate | Reuse the bounded `has_more`/`next_cursor` loop from notion-sync.mjs; workout data is small (§4.7) |
</threat_model>

<verification>
- `node --check` passes
- Live run writes four valid snapshots
- Worked-example acceptance test (Chest 3 / Triceps 7) confirmed by human
</verification>

<success_criteria>
WORKOUT-01/02/03/04 data foundations exist: four snapshots with correct per-muscle set counts (both attribution arrays), per-exercise series, session rows with parsed template/tags, and meta with current meso + bodyweight.
</success_criteria>

<artifacts_this_phase_produces>
- `scripts/notion-workouts-sync.mjs` (new sync script)
- `.dashboard-data/workouts-muscle-volume.json`, `workouts-exercises.json`, `workouts-sessions.json`, `workouts-meta.json` (generated snapshots)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-workouts-dashboard/07-01-SUMMARY.md` when done
</output>
