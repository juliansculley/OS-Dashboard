---
phase: 07-workouts-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - package.json
  - package-lock.json
  - src/types.ts
  - src/settings/SettingsTab.ts
  - src/utils/sanitizeText.ts
  - styles.css
autonomous: false
requirements: [WORKOUT-01, WORKOUT-02, WORKOUT-03, WORKOUT-04, WORKOUT-05]
user_setup: []
must_haves:
  truths:
    - "chart.js and dompurify are installed and resolvable; esbuild does NOT list chart.js in external"
    - "types.ts exports the four workout snapshot interfaces matching the §8 contract"
    - "Settings has a Workouts Data section with the sync script path + four snapshot paths + window/secondary-weight numeric settings"
    - "sanitizeText() returns plain text with HTML stripped, for safe use in Chart.js labels (SEC-01)"
    - "styles.css declares the 13 --cos-chart-* tokens inside .claudeos-dashboard plus all workouts component classes"
  artifacts:
    - path: "src/types.ts"
      provides: "MuscleVolumeSnapshot, ExercisesSnapshot, SessionsSnapshot, WorkoutsMetaSnapshot + supporting interfaces; extended ClaudeOSSettings + DEFAULT_SETTINGS; PageId union with 'workouts'"
      contains: "MuscleVolumeSnapshot"
    - path: "src/utils/sanitizeText.ts"
      provides: "sanitizeText(name): plain-text HTML-stripping sanitizer for Chart.js labels"
      exports: ["sanitizeText"]
    - path: "src/settings/SettingsTab.ts"
      provides: "Workouts Data settings section"
      contains: "Workouts Data"
    - path: "styles.css"
      provides: "--cos-chart-0..12 palette tokens + all .claudeos-workouts-* classes"
      contains: "--cos-chart-0"
  key_links:
    - from: "esbuild.config.mjs"
      to: "chart.js bundling"
      via: "chart.js absent from external array → bundled"
      pattern: "chart.js"
    - from: "src/utils/sanitizeText.ts"
      to: "dompurify"
      via: "DOMPurify.sanitize with empty allowlists"
      pattern: "dompurify|DOMPurify"
---

<objective>
Lay the foundation layer for the Workouts page: install Chart.js (D1) and DOMPurify (SEC-01), add the four §8 snapshot interfaces and extended settings to `types.ts`, extend `SettingsTab` with the Workouts Data section, create the `sanitizeText` utility, and add the full Workouts stylesheet (the 13 `--cos-chart-*` palette tokens plus every `.claudeos-workouts-*` class from the UI-SPEC). No page or tab components yet — those land in 07-04/05/06.

Purpose: WORKOUT-05 (manual refresh) needs `workoutsSyncScriptPath`; all tabs need the snapshot types, the CSS classes, and the sanitizer. Building CSS here (fully specified in the UI-SPEC) lets the leaf tab plans run in parallel without fighting over styles.css.

Output: installed deps, complete types, settings section, sanitize util, and complete Workouts CSS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md
@.planning/phases/07-workouts-dashboard/07-UI-SPEC.md
@.planning/phases/07-workouts-dashboard/07-RESEARCH.md
@src/types.ts
@src/settings/SettingsTab.ts
@src/views/DashboardView.tsx
@esbuild.config.mjs
@styles.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (chart.js, dompurify)</name>
  <what-built>Two npm packages to be installed: `chart.js` (charting, D1) and `dompurify` + `@types/dompurify` (SEC-01 string sanitizer for Chart.js labels). slopcheck was unavailable at research time, so both are [ASSUMED] and must be human-verified before install (research §Package Legitimacy Audit).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/chart.js — confirm it is the official chartjs/Chart.js library (~40M weekly downloads, MIT, repo github.com/chartjs/Chart.js), current stable 4.x.
    2. Open https://www.npmjs.com/package/dompurify — confirm it is the official cure53/DOMPurify library (widely used, Apache-2.0/MPL, repo github.com/cure53/DOMPurify).
    3. Confirm neither shows typosquat/SLOP signals (recent unknown publisher, near-zero downloads, name lookalike).
  </how-to-verify>
  <acceptance_criteria>
    - chart.js confirmed as the legitimate chartjs/Chart.js package
    - dompurify confirmed as the legitimate cure53/DOMPurify package
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize installing chart.js and dompurify, or "reject" with concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install deps + types + settings + sanitizeText util</name>
  <files>package.json, package-lock.json, src/types.ts, src/settings/SettingsTab.ts, src/utils/sanitizeText.ts</files>
  <read_first>
    - src/types.ts (existing PageId union, ClaudeOSSettings, DEFAULT_SETTINGS, SnapshotMeta — extend, do not rewrite)
    - src/settings/SettingsTab.ts (the setName/setDesc/addText + setHeading pattern, and the parseInt numeric pattern used by dueSoonDays)
    - src/views/DashboardView.tsx (renderSafeHTML / sanitizeHTMLToDom — existing SEC-01 mechanism, for reference)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§8 type contract; settings keys + defaults including muscleWindowDays=28, secondaryMuscleWeight=1.0)
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (Settings Contract table; the path defaults)
  </read_first>
  <action>
Install with the machine's required flag (corporate SSL): `npm install --strict-ssl=false chart.js dompurify` and `npm install --strict-ssl=false -D @types/dompurify`. Do NOT modify esbuild.config.mjs — chart.js must stay OUT of the external array so esbuild bundles + tree-shakes it (research §esbuild Configuration).

In `src/types.ts`:
- Extend the `PageId` union to add `'workouts'`: `export type PageId = 'home' | 'social' | 'projects' | 'newsletter' | 'workouts';`
- Add the §8 interfaces (reuse the existing `SnapshotMeta`): `MuscleWeekCell { week: string; muscle: string; sets: number }`, `SessionMuscleBreakdown { session_id: string; date: string; name: string; sets_by_muscle: Record<string, number> }`, `MuscleVolumeSnapshot extends SnapshotMeta { muscles: string[]; weekly_with_secondary: MuscleWeekCell[]; weekly_primary_only: MuscleWeekCell[]; by_session: SessionMuscleBreakdown[]; window_default_days: number; secondary_weight: number }`, `ExercisePoint { date: string; weight: number|null; volume: number|null; est_1rm: number|null }`, `ExerciseSeries { exercise_id: string; name: string; primary: string[]; secondary: string[]; best_weight: number|null; best_1rm: number|null; points: ExercisePoint[] }`, `ExercisesSnapshot extends SnapshotMeta { exercises: ExerciseSeries[] }`, `SessionRow { session_id: string; date: string; name: string; template?: string; tags: string[]; difficulty: string[]; results: string[]; location?: string; meso_name?: string; total_sets: number; url: string }`, `SessionsSnapshot extends SnapshotMeta { sessions: SessionRow[] }`, `BodyPoint { date: string; value: number }`, `WorkoutsMetaSnapshot extends SnapshotMeta { current_meso?: { name: string; wo_type: string; focus: string; status: string; end: string }; bodyweight: BodyPoint[] }`. Field names must match 07-01 snapshot output exactly — cross-check against the 07-01 SUMMARY.
- Extend `ClaudeOSSettings` and `DEFAULT_SETTINGS` with: `workoutsSyncScriptPath: ""`, `workoutsMuscleVolumePath: "OS-Dashboard/.dashboard-data/workouts-muscle-volume.json"`, `workoutsExercisesPath: "OS-Dashboard/.dashboard-data/workouts-exercises.json"`, `workoutsSessionsPath: "OS-Dashboard/.dashboard-data/workouts-sessions.json"`, `workoutsMetaPath: "OS-Dashboard/.dashboard-data/workouts-meta.json"`, `muscleWindowDays: 28`, `secondaryMuscleWeight: 1.0`. (Match the existing snapshot-path default prefix `OS-Dashboard/.dashboard-data/` used by the Phase 3 defaults.)

In `src/settings/SettingsTab.ts`: add a new `new Setting(containerEl).setName('Workouts Data').setHeading()` section below the existing Notion Sync section. Add a text setting for each of the five workout path keys (label/desc per UI-SPEC Settings Contract), and two numeric settings (`muscleWindowDays` default 28, `secondaryMuscleWeight` default 1.0) using the existing parseInt/parseFloat-with-fallback pattern (secondaryMuscleWeight uses parseFloat; clamp to >= 0).

Create `src/utils/sanitizeText.ts` exporting `sanitizeText(input: string): string` that returns plain text with all HTML stripped: `import DOMPurify from 'dompurify'; export function sanitizeText(input: string): string { return DOMPurify.sanitize(input ?? '', { ALLOWED_TAGS: [], ALLOWED_ATTR: [] }); }`. This is the SEC-01 sanitizer for Chart.js labels (canvas does not benefit from React's auto-escaping). Add a doc comment noting it is for non-React render surfaces (Chart.js labels); React text children remain auto-escaped.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (all new types compile)
    - `chart.js` and `dompurify` appear in package.json dependencies; `@types/dompurify` in devDependencies
    - esbuild.config.mjs external array does NOT contain `chart.js` (grep -v '^//' esbuild.config.mjs | grep -c '"chart.js"' returns 0)
    - types.ts exports MuscleVolumeSnapshot, ExercisesSnapshot, SessionsSnapshot, WorkoutsMetaSnapshot
    - PageId union includes 'workouts'
    - src/utils/sanitizeText.ts exports sanitizeText and calls DOMPurify.sanitize with empty ALLOWED_TAGS/ALLOWED_ATTR
    - SettingsTab contains a 'Workouts Data' heading and all five workout path settings + muscleWindowDays + secondaryMuscleWeight
  </acceptance_criteria>
  <done>Deps installed and bundled (not external), all four snapshot interfaces + settings + PageId extension compile, sanitizeText util exists, Workouts Data settings section present.</done>
</task>

<task type="auto">
  <name>Task 3: Add Workouts CSS — chart palette tokens + all component classes</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (read the full .claudeos-dashboard token block at the top; the existing .claudeos-section-heading, .claudeos-tile, .claudeos-empty-state, .claudeos-refresh-bar, .claudeos-list-row, .claudeos-stale-label rules to match conventions)
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (Color → Chart.js Color Palette table with the 13 hex values; CSS Class Reference table listing every selector and its purpose; Layout/Tab bar/control specs)
  </read_first>
  <action>
Inside the existing `.claudeos-dashboard { ... }` token block (alongside the other `--cos-*` declarations), add the 13 chart palette tokens with the exact hex values from the UI-SPEC Color table: `--cos-chart-0: #7c6af7;` through `--cos-chart-12: #b794f4;` (0 Chest, 1 Back #38a169, 2 Triceps #e53e3e, 3 Biceps #dd6b20, 4 Shoulders #3182ce, 5 Side Delt #805ad5, 6 Quadriceps #d69e2e, 7 Hamstrings #319795, 8 Glutes #e84393, 9 Lower Back #718096, 10 Traps #63b3ed, 11 Abs #68d391, 12 Calves #b794f4). They MUST live under `.claudeos-dashboard` (not :root) so getComputedStyle resolves them when the canvas is inside the wrapper (research Pattern 4).

Add every selector from the UI-SPEC CSS Class Reference table, all scoped under `.claudeos-dashboard` (D-10): page wrapper `.claudeos-page--workouts` (flex column, gap 24px); tab bar `.claudeos-workouts-tabs` (flex row, border-bottom 1px var(--cos-border), surface bg), `.claudeos-workouts-tab` (12px/600/uppercase, letter-spacing 0.06em, padding 8px 16px, color var(--cos-muted)), `:hover` (surface-2 bg, text color, transition background 150ms), `.active` (color var(--cos-accent), border-bottom 3px solid var(--cos-accent)); `.claudeos-workouts-tab-content` (flex 1, overflow-y auto, padding-top 16px); controls `.claudeos-workouts-controls`, `.claudeos-workouts-control-row`, `.claudeos-workouts-control-label`, `.claudeos-workouts-btn-group`, `.claudeos-workouts-preset-btn` (surface bg, border, 12px/600), `.active` (surface-2 bg, accent border, accent text), `:disabled` (faint text, no pointer-events); chart `.claudeos-workouts-chart-container` (surface bg, border, radius, padding 16px), `.claudeos-workouts-chart-label` (16px/600); selects `.claudeos-workouts-exercise-select`, `.claudeos-workouts-filter-select` (full-width, 14px, border, radius, padding 8px 16px, bg var(--cos-bg)); filters `.claudeos-workouts-filter-row`, `.claudeos-workouts-filter-group`, `.claudeos-workouts-split-btns`, `.claudeos-workouts-split-btn`, `.active`; PR tiles `.claudeos-pr-tiles` (flex, gap 16px), `.claudeos-pr-tile`, `.claudeos-pr-tile__label` (12px/600/uppercase/muted), `.claudeos-pr-tile__value` (22px/600); meso card `.claudeos-workouts-meso-card`, `__label`, `__name` (16px/600), `__detail` (14px/400/muted), `__overdue` (error color); sparkline `.claudeos-workouts-sparkline-label`, `.claudeos-workouts-sparkline-latest`. Use existing tokens for all values (no new hardcoded hex except the chart palette tokens). Match spacing tokens per the UI-SPEC Spacing Scale.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('styles.css','utf8'); const need=['--cos-chart-0','--cos-chart-12','claudeos-page--workouts','claudeos-workouts-tabs','claudeos-workouts-tab-content','claudeos-workouts-preset-btn','claudeos-workouts-chart-container','claudeos-workouts-exercise-select','claudeos-workouts-filter-row','claudeos-pr-tile','claudeos-workouts-meso-card','claudeos-workouts-sparkline-label']; const missing=need.filter(s=>!c.includes(s)); if(missing.length){console.error('MISSING:',missing.join(', ')); process.exit(1)} console.log('all workouts CSS selectors present')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify script prints "all workouts CSS selectors present" and exits 0
    - All 13 --cos-chart-N tokens (0..12) declared inside .claudeos-dashboard, not :root
    - Every selector in the UI-SPEC CSS Class Reference table exists and is scoped under .claudeos-dashboard
    - No hardcoded hex values other than the 13 chart palette tokens
  </acceptance_criteria>
  <done>styles.css contains the chart palette tokens and every Workouts component class, all scoped under .claudeos-dashboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Two new packages enter the bundle |
| snapshot strings → Chart.js label | Notion strings reach a non-React render surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | npm installs (chart.js, dompurify) | mitigate | slopcheck + blocking-human checkpoint (Task 1) before install; verify on npmjs.com |
| T-07-06 | Tampering | Notion strings in Chart.js labels | mitigate | sanitizeText() (DOMPurify, empty allowlists) strips HTML before labels — consumed by 07-04/05/06 (SEC-01) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with new types
- chart.js absent from esbuild external
- All Workouts CSS selectors present
</verification>

<success_criteria>
The Workouts feature's type, settings, sanitizer, and style foundations exist and compile; Chart.js + DOMPurify are installed and bundle-ready. WORKOUT-05's `workoutsSyncScriptPath` setting exists.
</success_criteria>

<artifacts_this_phase_produces>
- `src/utils/sanitizeText.ts` (new SEC-01 plain-text sanitizer)
- `src/types.ts` (modified — four snapshot interfaces, extended settings, PageId)
- `src/settings/SettingsTab.ts` (modified — Workouts Data section)
- `styles.css` (modified — chart palette tokens + all workouts classes)
- `package.json` / `package-lock.json` (modified — chart.js, dompurify)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-workouts-dashboard/07-03-SUMMARY.md` when done
</output>
