---
phase: 07-workouts-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/exercise-muscle-map.json
  - scripts/notion-workouts-sync.mjs
autonomous: false
requirements: [WORKOUT-01]
must_haves:
  truths:
    - "scripts/exercise-muscle-map.json exists and is valid JSON after clone (never crashes a fresh checkout)"
    - "The sync script seeds primary[] from Notion Prime muscle every run, and reads secondary[] from the map file"
    - "When an exercise is absent from the map, the script falls back to parsing Notion Secondary muscle text, else empty — never crashes"
    - "After wiring, bench press contributes to BOTH Chest (primary) and Triceps (secondary) in the muscle-volume snapshot"
  artifacts:
    - path: "scripts/exercise-muscle-map.json"
      provides: "Override map of exercise → {primary[], secondary[]} for common lifts"
      contains: "secondary"
    - path: "scripts/notion-workouts-sync.mjs"
      provides: "Fallback chain: override file → Notion Secondary muscle text parse → empty"
      contains: "exercise-muscle-map"
  key_links:
    - from: "scripts/notion-workouts-sync.mjs"
      to: "scripts/exercise-muscle-map.json"
      via: "readFile at startup with graceful-degradation try/catch"
      pattern: "exercise-muscle-map"
---

<objective>
Create the `exercise-muscle-map.json` enrichment layer and wire its three-step fallback chain into the sync script, so secondary-muscle attribution is correct (Notion's Secondary muscle field is sparse free text — design §4.3). Per D4, the build phase seeds the map (primary auto-seeded from Notion; secondary hand-filled from a standard exercise reference for the user's actual exercise list), and the user edits it over time.

Purpose: WORKOUT-01's worked example REQUIRES secondary attribution (bench → triceps). Without this layer, triceps/shoulders/etc are undercounted.

Output: A populated `scripts/exercise-muscle-map.json` and a sync script that consumes it with graceful degradation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md
@scripts/notion-workouts-sync.mjs
@.planning/phases/07-workouts-dashboard/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exercise-muscle-map.json with secondary assignments</name>
  <files>scripts/exercise-muscle-map.json</files>
  <read_first>
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§6.3 map shape and build rule; §3.4 valid muscle group names; §10 D4)
    - .dashboard-data/workouts-exercises.json (the live exercise list produced by 07-01 — use its actual exercise names/ids as keys so the map matches real data)
  </read_first>
  <action>
Create `scripts/exercise-muscle-map.json` keyed by the normalized exercise name (lowercase, trimmed) — the same normalization the sync script will use for lookup. Each value is `{ "primary": [...], "secondary": [...] }`. `primary` may be left empty in the file (the script auto-seeds it from Notion Prime muscle every run and that value wins); the file's real job is `secondary`. Use ONLY the valid muscle group names from design §3.4 with exact casing: Chest, Back, Lower Back, Shoulders, Side Delt, Biceps, Triceps, Quadriceps, Hamstrings, Glutes, Calves, Traps, Abs. Keep `Side Delt` and `Shoulders` as separate entries — never merge (D3). Do NOT use Cardio or knee.

Populate secondary for the common compound lifts present in the live exercise list (read .dashboard-data/workouts-exercises.json for the exact names), using a standard exercise→muscle reference, e.g.:
- bench press → secondary: ["Triceps", "Shoulders"]
- incline bench press → secondary: ["Triceps", "Shoulders"]
- overhead press / ohp → secondary: ["Triceps"]
- squat / smith squat → secondary: ["Glutes", "Hamstrings"]
- deadlift → secondary: ["Back", "Lower Back", "Glutes"]
- barbell row / row → secondary: ["Biceps"]
- pull up / pulldown → secondary: ["Biceps"]
- dip → secondary: ["Chest", "Shoulders"]
Cover the user's actual most-used exercises from the live snapshot; isolation movements (curls, extensions, raises) get empty secondary `[]`. The file MUST be valid JSON (no comments, no trailing commas).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('scripts/exercise-muscle-map.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(...)"` exits 0 (valid JSON)
    - At least the bench-press key exists with `Triceps` in its secondary array
    - No `Cardio` or `knee` value appears anywhere in the file
    - `Side Delt` and `Shoulders` are never merged into a single token
  </acceptance_criteria>
  <done>A valid `exercise-muscle-map.json` exists with secondary muscle assignments for the user's common compound lifts, keyed to the live exercise names.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the fallback chain into the sync script</name>
  <files>scripts/notion-workouts-sync.mjs</files>
  <read_first>
    - scripts/notion-workouts-sync.mjs (the primary-muscle seeding + per-line join from 07-01 — add the secondary resolution at that extension point)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§6.3 build rule: override → Notion text parse → empty)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (exercise-muscle-map.json Bootstrap Strategy — graceful degradation, file lives in scripts/ next to the script)
  </read_first>
  <action>
At script startup, load `scripts/exercise-muscle-map.json` via `readFile` resolved relative to the script dir (`join(scriptDir, 'exercise-muscle-map.json')`). Wrap in try/catch: if the file is absent or unparseable, write a warning to stderr (e.g. "exercise-muscle-map.json not found — secondary muscle attribution will fall back to Notion text") and continue with an empty map `{}` — do NOT crash (graceful degradation, research Bootstrap Strategy).

In the exercise muscle resolution (where 07-01 left the secondary extension point), implement the three-step fallback for each exercise's `secondary[]`:
1. If the normalized exercise name is a key in the loaded map and has a non-empty `secondary` array → use it.
2. Else parse Notion's `Secondary muscle` plain-text field (`props['Secondary muscle'].rich_text` joined to a string): split on commas/whitespace, case-insensitively match each token against the known muscle-group name set, keep matches.
3. Else `secondary: []`.

`primary[]` continues to come from the Notion `Prime muscle` relation every run (always authoritative — never overridden by the file). Normalize exercise names consistently (lowercase + trim) for both the map keys and the lookup.
  </action>
  <verify>
    <automated>node --check scripts/notion-workouts-sync.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/notion-workouts-sync.mjs` exits 0
    - Grep confirms `exercise-muscle-map` referenced in the script and wrapped in try/catch (file contains both `exercise-muscle-map` and `catch`)
    - The script still seeds primary from `Prime muscle` (grep `Prime muscle` present)
    - Missing-file path logs a warning and continues (no `process.exit` inside the map-load catch block)
  </acceptance_criteria>
  <done>The sync script reads the enrichment map with graceful degradation and resolves secondary muscles via override → Notion text parse → empty.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run and re-verify worked example with secondary attribution</name>
  <what-built>The sync script now consuming the enrichment map.</what-built>
  <how-to-verify>
    1. Re-run: `& "C:\Users\scull\AppData\Local\nvm\v24.12.0\node.exe" scripts\notion-workouts-sync.mjs`
    2. Open `.dashboard-data\workouts-muscle-volume.json`. Confirm bench-press sessions now contribute to Triceps (and Shoulders) via secondary, not just Chest.
    3. Confirm the worked-example total still holds: a window with 3 sets bench (Chest/Triceps) + 4 sets tricep extension (Triceps) = Chest 3, Triceps 7.
    4. Confirm `weekly_primary_only` excludes the secondary contributions (bench adds to Chest only there), while `weekly_with_secondary` includes them.
  </how-to-verify>
  <acceptance_criteria>
    - Secondary attribution now visible in weekly_with_secondary
    - weekly_primary_only differs from weekly_with_secondary for exercises with secondary muscles
    - Worked example (Chest 3 / Triceps 7) holds
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any attribution mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Denial of Service | Missing/corrupt map file | mitigate | try/catch around map load; warn + continue with empty map (graceful degradation) |
| T-07-05 | Tampering | Map values used in aggregation | accept | Map is a local developer-maintained file, not user runtime input; muscle names validated against the known set |
</threat_model>

<verification>
- `node --check` passes
- Live re-run shows secondary attribution and preserves the worked example
</verification>

<success_criteria>
WORKOUT-01 muscle-group set volume is correct with full secondary attribution; the enrichment layer degrades gracefully when the file is absent.
</success_criteria>

<artifacts_this_phase_produces>
- `scripts/exercise-muscle-map.json` (new enrichment map)
- `scripts/notion-workouts-sync.mjs` (modified — fallback chain wired)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-workouts-dashboard/07-02-SUMMARY.md` when done
</output>
