---
phase: 07-workouts-dashboard
plan: 05
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - src/components/pages/workouts/HistoryTab.tsx
  - src/components/pages/workouts/ContextTab.tsx
autonomous: true
requirements: [WORKOUT-03, WORKOUT-04]
must_haves:
  truths:
    - "HistoryTab renders sessions reverse-chronologically as ListRows with date/tags/difficulty/total-sets badges, each linking to Notion"
    - "History filters (split multi-select, difficulty, results, mesocycle) narrow the list client-side with AND-across / OR-within semantics"
    - "Negative WO results badges (joint pain, None) render with the error tint"
    - "ContextTab renders the current mesocycle card with days-remaining and a bodyweight sparkline"
  artifacts:
    - path: "src/components/pages/workouts/HistoryTab.tsx"
      provides: "Filter row + session history list (ListRow)"
      exports: ["HistoryTab"]
    - path: "src/components/pages/workouts/ContextTab.tsx"
      provides: "Mesocycle card + bodyweight sparkline"
      exports: ["ContextTab"]
  key_links:
    - from: "HistoryTab"
      to: "ListRow"
      via: "reuse existing ListRow component with badges + Notion url"
      pattern: "ListRow"
    - from: "ContextTab"
      to: "Chart.js sparkline"
      via: "useEffect destroy-before-create + Filler fill"
      pattern: "chartRef.*destroy"
---

<objective>
Build the remaining two leaf tab components: `HistoryTab` (filter row + reverse-chronological session list using the existing `ListRow`) and `ContextTab` (current mesocycle card + bodyweight sparkline). These compile independently against snapshot data passed by the parent; the WorkoutsPage shell wiring happens in 07-06. This plan touches only its two new files, so it runs in parallel with 07-04.

Purpose: WORKOUT-03 (session history with filters) and WORKOUT-04 (mesocycle context) render here. Per D7, all filtering is client-side over the loaded snapshot.

Output: two new component files implementing the UI-SPEC §6 and §7 contracts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-workouts-dashboard/07-UI-SPEC.md
@.planning/phases/07-workouts-dashboard/07-RESEARCH.md
@C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md
@src/components/ui/ListRow.tsx
@src/types.ts
@src/utils/sanitizeText.ts
@src/context/AppContext.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HistoryTab — filter row + session list</name>
  <files>src/components/pages/workouts/HistoryTab.tsx</files>
  <read_first>
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (§6 HistoryTab: filter row 6a, session table 6b, empty states 6c; Interaction Contract → session history filters AND/OR semantics; Copywriting Contract: SPLIT/DIFFICULTY/RESULTS/MESOCYCLE labels, "All" default, history empty-state copy; results-badge error tint rule)
    - src/components/ui/ListRow.tsx (props: name, url, badges[], emphasis — reuse as-is; row name shows the session template not the raw Name)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§8 SessionRow/SessionsSnapshot)
    - src/types.ts (SessionsSnapshot, SessionRow)
    - src/utils/sanitizeText.ts
    - src/components/pages/ProjectsPage.tsx (inline EmptyState pattern to copy)
  </read_first>
  <action>
Create `export function HistoryTab(props: { sessions: SessionsSnapshot })`. Copy the inline `EmptyState` component pattern from ProjectsPage. Local filter state: `splitTags: string[]` (multi-select from Push/Pull/Full/Arms/Hyp/Str), `difficulty: string` ('All' default), `results: string` ('All' default), `meso: string` ('All' default).

Filter row (UI-SPEC 6a) under a FILTERS label: a SPLIT multi-select pill group (`.claudeos-workouts-split-btn`/`.active`) for the six tag tokens; a DIFFICULTY native `<select class="claudeos-workouts-filter-select">` with options 'All' + unique difficulty values across sessions; a RESULTS select with 'All' + unique results values; a MESOCYCLE select with 'All' + unique meso_name values sorted descending by first session date. Each group is a `.claudeos-workouts-filter-group` with a 12px/600/uppercase label.

Filtering (Interaction Contract — AND across filter types, OR within the multi-select): apply each active filter; SPLIT matches if the session's tags[] intersect the selected splitTags (when splitTags non-empty); difficulty/results match when the session's difficulty[]/results[] include the selected value (when not 'All'); meso matches meso_name (when not 'All'). Sort the filtered list reverse-chronologically by date.

Render each filtered session as a `ListRow`: `name` = the session `template` (fall back to `name` if template absent), sanitized via `sanitizeText`; `url` = session.url (ListRow already adds rel="noopener noreferrer" target="_blank"); `badges` = [date, ...tags, ...difficulty, `${total_sets} sets`]. For sessions whose `results` contains a negative value (`joint pain` or `None`), surface that result with the error tint — pass `emphasis="overdue"` to ListRow (which maps to the error-tinted modifier) OR add the result as a badge and rely on a results-specific class; choose the approach that reuses ListRow without modifying it, and document the choice. If the list exceeds 100 rows, wrap the list container with `max-height: 400px; overflow-y: auto` (inline style or an existing scroll class).

Empty states (UI-SPEC 6c) with exact copy: sessions null → "No session data" / "Click Refresh to pull workout sessions from Notion." (the null/error cases are handled by the parent in 07-06, but render the local cases here): zero total sessions → EmptyState "No sessions yet" / "Log your first workout in Notion, then Refresh."; filter produces zero rows → inline `claudeos-list-empty` "No sessions match the active filters. Clear a filter to see more."
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Reuses ListRow without modifying ListRow.tsx
    - Filter logic implements AND-across-types / OR-within-split
    - Session name uses template, sanitized via sanitizeText
    - Negative results (joint pain / None) get error tint
    - Empty-state copy matches the UI-SPEC exactly ("No sessions yet", "No sessions match the active filters. Clear a filter to see more.")
    - 100+ row guard applies max-height scroll
  </acceptance_criteria>
  <done>HistoryTab renders the filterable, reverse-chronological session list with the correct badge columns, error-tinted negative results, and exact empty-state copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ContextTab — mesocycle card + bodyweight sparkline</name>
  <files>src/components/pages/workouts/ContextTab.tsx</files>
  <read_first>
    - .planning/phases/07-workouts-dashboard/07-UI-SPEC.md (§7 ContextTab: meso card 7a, sparkline 7b; Copywriting Contract: CURRENT MESOCYCLE, BODYWEIGHT, latest line format, no-meso/no-bodyweight copy; days-remaining + overdue rule)
    - .planning/phases/07-workouts-dashboard/07-RESEARCH.md (Pattern 3 canvas lifecycle, Pattern 4 CSS var resolution)
    - C:\Users\scull\OneDrive\ClaudeOS\OS-Dashboard\.planning\WORKOUTS-DASHBOARD-DESIGN.md (§8 WorkoutsMetaSnapshot, BodyPoint)
    - src/types.ts (WorkoutsMetaSnapshot, BodyPoint)
    - src/components/pages/ProjectsPage.tsx (inline EmptyState pattern)
  </read_first>
  <action>
Create `export function ContextTab(props: { meta: WorkoutsMetaSnapshot })`. Copy the inline EmptyState pattern.

Mesocycle card (UI-SPEC 7a) `.claudeos-workouts-meso-card`: label "CURRENT MESOCYCLE"; if `meta.current_meso` present render name (`__name`), detail lines `__detail` ("Type: {wo_type} · Focus: {focus}" and "Status: {status}"), and the end/days-remaining line. Compute days remaining = current_meso.end minus today (date-only). If positive render "Ends: {end}  ({N} days remaining)"; if negative render "Ended {N} days ago" in the `__overdue` (error) class. If `current_meso` is absent render EmptyState "No active mesocycle" / "No mesocycle block covers today's date."

Bodyweight sparkline (UI-SPEC 7b): label "BODYWEIGHT (last 60 days)"; if `meta.bodyweight` non-empty render a Chart.js line sparkline — `type:'line'`, canvas height 100px, x/y axes hidden (`display:false`), line color `--cos-chart-9` resolved at mount via getComputedStyle (research Pattern 4), pointRadius 2, `fill:'origin'` with `backgroundColor: 'rgba(113,128,150,0.10)'` (requires the Filler plugin registered in 07-04's chartSetup), `animation:false`, legend hidden, tooltip "{date}: {value} lbs". Window = last 60 points sorted ascending by date. Below the sparkline render the latest value text `.claudeos-workouts-sparkline-latest`: "{latest_value} lbs  as of {latest_date}". If bodyweight is empty render inline `claudeos-list-empty` "No bodyweight measurements found." Canvas lifecycle: destroy-before-create + cleanup destroy (research Pattern 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - Sparkline uses destroy-before-create + cleanup destroy (grep chartRef.*destroy)
    - getComputedStyle resolves --cos-chart-9
    - Days-remaining computed; overdue path renders in the error class
    - Exact copy: "CURRENT MESOCYCLE", "No active mesocycle", "No mesocycle block covers today's date.", "No bodyweight measurements found.", latest line "{value} lbs  as of {date}"
    - Sparkline window = last 60 points
  </acceptance_criteria>
  <done>ContextTab renders the mesocycle card with days-remaining/overdue handling and the bodyweight sparkline with correct fill, axes-hidden, and canvas lifecycle.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering (XSS) | Session name / sparkline tooltip strings | mitigate | sanitizeText() on session template/name; ListRow renders name as React text child (auto-escaped); URLs only in href with rel="noopener noreferrer" (SEC-01) |
| T-07-11 | DoS (memory) | Sparkline canvas re-open | mitigate | destroy-before-create + cleanup destroy (research Pattern 3) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for both components
- ListRow reused unmodified
- Sparkline canvas destroy pattern present
</verification>

<success_criteria>
WORKOUT-03 session history (filterable, Notion-linked) and WORKOUT-04 mesocycle/bodyweight context render from snapshot data with the exact UI-SPEC copy and the canvas lifecycle.
</success_criteria>

<artifacts_this_phase_produces>
- `src/components/pages/workouts/HistoryTab.tsx` (new)
- `src/components/pages/workouts/ContextTab.tsx` (new)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-workouts-dashboard/07-05-SUMMARY.md` when done
</output>
