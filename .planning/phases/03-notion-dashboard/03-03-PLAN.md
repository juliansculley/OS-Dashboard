---
phase: 03-notion-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/ui/RefreshButton.tsx
  - src/components/ui/ListRow.tsx
  - src/components/pages/ProjectsPage.tsx
  - src/components/App.tsx
  - src/components/ui/Sidebar.tsx
  - styles.css
autonomous: true
requirements: [NOTION-04, NOTION-05, NOTION-08]
requirements_addressed: [NOTION-04, NOTION-05, NOTION-08]

must_haves:
  truths:
    - "Clicking Refresh runs the sync script via execFile(nodePath, [syncScriptPath]) and transitions idle→loading→success/error"
    - "On success, Refresh calls triggerRefresh() and the Projects page re-reads its snapshots and re-renders without an Obsidian reload"
    - "Refresh shows 'Last synced HH:mm' from the latest snapshot's generated_at"
    - "Projects page shows active projects (name, status, progress%) and a tasks list with an Overdue & Due Soon section above the remaining active tasks"
    - "Every task/project row links to its Notion page via an external-link anchor"
    - "Projects page degrades to a no-data state when snapshots are missing or unreadable; a stale (>24h) snapshot shows a 'Stale' label but still renders"
    - "Projects appears in the sidebar nav and routes to the Projects page"
  artifacts:
    - path: "src/components/ui/RefreshButton.tsx"
      provides: "execFile-based refresh state machine + last-synced display"
      contains: "execFile"
    - path: "src/components/ui/ListRow.tsx"
      provides: "Shared row: name, metadata badge(s), external Notion link"
      contains: "target=\"_blank\""
    - path: "src/components/pages/ProjectsPage.tsx"
      provides: "Projects + tasks rendering with overdue/due-soon emphasis and no-data/stale states"
      contains: "refreshNonce"
    - path: "src/components/App.tsx"
      provides: "PAGES map includes projects"
      contains: "ProjectsPage"
  key_links:
    - from: "src/components/ui/RefreshButton.tsx"
      to: "child_process.execFile"
      via: "execFile(settings.nodePath, [settings.syncScriptPath]) — no shell, no interpolation"
      pattern: "execFile\\("
    - from: "src/components/ui/RefreshButton.tsx"
      to: "triggerRefresh"
      via: "AppContext on success callback"
      pattern: "triggerRefresh\\(\\)"
    - from: "src/components/pages/ProjectsPage.tsx"
      to: "readJsonFile"
      via: "useEffect with refreshNonce in deps reads tasksSnapshotPath + projectsSnapshotPath"
      pattern: "readJsonFile"
---

<objective>
Build the refresh mechanism and the Projects page. `RefreshButton` clones `SkillButton`'s state machine but runs the sync script via `execFile(nodePath, [syncScriptPath])` and, on success, calls `triggerRefresh()` to bump the nonce so subscribed pages re-read snapshots. The Projects page renders active projects and tasks (with overdue/due-soon emphasis), links each row to Notion, and degrades to the established no-data/stale state. A shared `ListRow` component handles task/project/newsletter rows (Plan 4 reuses it).

Purpose: Deliver NOTION-04 (refresh + live reload) and NOTION-05 (Projects page) on the contracts Plan 2 codified.
Output: `RefreshButton.tsx`, `ListRow.tsx`, `ProjectsPage.tsx`, wired into `App.tsx`/`Sidebar.tsx`, with scoped CSS.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-notion-dashboard/03-CONTEXT.md
@.planning/phases/03-notion-dashboard/03-02-SUMMARY.md
@src/components/ui/SkillButton.tsx
@src/components/pages/SocialPage.tsx
@src/components/ui/SocialMetricCard.tsx
@src/components/pages/HomePage.tsx
@src/components/App.tsx
@src/components/ui/Sidebar.tsx
@src/context/AppContext.tsx
@src/utils/readJsonFile.ts
@src/types.ts
@styles.css
</context>

<artifacts_this_phase_produces>
New symbols (Phase 3 inventory, Plan 3 of 4):

- `src/components/ui/RefreshButton.tsx` — `RefreshButton` component. Props: none required (reads settings + context internally). Internal state `'idle' | 'loading' | 'success' | 'error'`. Renders a button + a "Last synced HH:mm" label.
- `src/components/ui/ListRow.tsx` — `ListRow` component. Props: `{ name: string; url: string; badges?: string[]; emphasis?: 'overdue' | 'due-soon' | 'none' }`. Renders an `<a>` opening the Notion URL in a new tab, the name as text (XSS-safe via JSX text node — D-17), and metadata badges.
- `src/components/pages/ProjectsPage.tsx` — `ProjectsPage` component. Reads tasks + projects snapshots, renders RefreshButton, projects list, an "Overdue & Due Soon" tasks section, then remaining active tasks. Handles no-data and stale states.
- `src/components/App.tsx`: `PAGES` map gains `projects: ProjectsPage`.
- `src/components/ui/Sidebar.tsx`: `NAV_ITEMS` gains a Projects entry.
- `styles.css`: scoped rules for `.claudeos-refresh-btn*`, `.claudeos-list-row*`, `.claudeos-page--projects`, stale/no-data labels — all under `.claudeos-dashboard`.

Shared helper to define once (used here and in Plan 4): a stale check comparing `generated_at` to now and a "HH:mm" formatter. Co-locate in RefreshButton or a small util; Plan 4 reuses it for the Newsletter page.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build RefreshButton (execFile state machine + last-synced) and ListRow</name>
  <files>src/components/ui/RefreshButton.tsx, src/components/ui/ListRow.tsx</files>
  <read_first>
    - src/components/ui/SkillButton.tsx (the exact idle/loading/success/error state machine, IconSlot, spinner, auto-reset timers, execFile usage to clone)
    - src/context/AppContext.tsx (useAppContext now returns refreshNonce + triggerRefresh)
    - src/types.ts (ClaudeOSSettings.nodePath, syncScriptPath, the three snapshot paths; SnapshotMeta.generated_at)
    - src/utils/readJsonFile.ts (to read the latest snapshot's generated_at for the last-synced label)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-09, D-10, D-17; the "Last synced HH:mm" requirement; SEC-03 posture)
  </read_first>
  <action>
    Create `src/components/ui/RefreshButton.tsx`. Clone SkillButton's structure: a `RefreshState = 'idle' | 'loading' | 'success' | 'error'` state, the `IconSlot` + spinner pattern, and the auto-reset timers (success → idle after 3000ms, error → idle after 5000ms). Read `app`, `plugin`, and `triggerRefresh` from `useAppContext()`.

    On click (guard: ignore if state !== 'idle'): read `const node = plugin.settings.nodePath || 'node'` and `const script = plugin.settings.syncScriptPath`. If `script` is empty/blank, set state to 'error' immediately (the user has not configured the script path — D-09) and return without calling exec. Otherwise set state 'loading' and call `execFile(node, [script], (error) => {...})` from `child_process` (NOTION-04, critical finding 4: two separate args — the node executable and the script path; NO shell, NO string interpolation, NO user input beyond the two configured paths — SEC-03). In the callback: if `error === null`, set state 'success', call `triggerRefresh()` (bumps the nonce so pages re-read), refresh the last-synced label (re-read generated_at), then auto-reset; else set state 'error' then auto-reset.

    Last-synced label (D-10): keep a `lastSynced: string | null` state. On mount and after a successful refresh, read the tasks snapshot (`plugin.settings.tasksSnapshotPath`) via `readJsonFile<SnapshotMeta>`, take `generated_at`, and format to `HH:mm` (zero-padded hours and minutes from a `new Date(generated_at)`; guard `isNaN`). Render the label as `Last synced HH:mm` when non-null, else render nothing. Add a stale helper `isStale(generatedAt)`: returns true when `Date.now() - new Date(generatedAt).getTime() > 24*60*60*1000`. Export both the component and the `isStale` + `formatHHmm` helpers (named exports) so ProjectsPage and the Plan 4 NewsletterPage reuse them.

    Render a `<button className="claudeos-refresh-btn claudeos-refresh-btn--{state}">` with a refresh icon (use `IconSlot` with `refresh-cw` for idle, `loader-2` spinner for loading, `check` for success, `x` for error) and label text ("Refresh" / loading has no text / "Synced" / "Failed"). Place the last-synced label adjacent in a wrapping `<div className="claudeos-refresh-bar">`.

    Create `src/components/ui/ListRow.tsx`. Props `{ name: string; url: string; badges?: string[]; emphasis?: 'overdue' | 'due-soon' | 'none' }`. Render `<a className="claudeos-list-row claudeos-list-row--{emphasis}" href={url} target="_blank" rel="noopener noreferrer">`. Inside: a `<span className="claudeos-list-row__name">{name}</span>` (name set as a JSX text child — XSS-safe natively, D-17; do NOT use dangerouslySetInnerHTML), then a `<span className="claudeos-list-row__badges">` mapping `badges` to `<span className="claudeos-list-row__badge">`, then a trailing external-link icon via `IconSlot` (`external-link`). Default `emphasis` to `'none'` and `badges` to `[]`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `RefreshButton.tsx` imports `execFile` from `child_process` and calls `execFile(node, [script], ...)` with two positional args — no template literal, no `exec`, no `shell: true`.
    - When `syncScriptPath` is blank the click handler sets the error state and does NOT call execFile.
    - On the success callback the code calls `triggerRefresh()`.
    - The component renders a "Last synced" label derived from a snapshot `generated_at`, formatted HH:mm.
    - `RefreshButton.tsx` exports `isStale` and an HH:mm formatter as named exports.
    - `ListRow.tsx` renders an `<a>` with `target="_blank"` and `rel="noopener noreferrer"`, sets `name` as a JSX text child (no `dangerouslySetInnerHTML` anywhere in the file).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>RefreshButton runs the script safely via execFile, triggers a nonce bump on success, shows last-synced; ListRow renders safe external links. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build ProjectsPage with overdue/due-soon emphasis, no-data and stale states</name>
  <files>src/components/pages/ProjectsPage.tsx</files>
  <read_first>
    - src/components/pages/SocialPage.tsx (the T | null | 'error' no-data/error union pattern to clone)
    - src/components/ui/SocialMetricCard.tsx (the empty-state markup: claudeos-empty-state heading/body)
    - src/components/pages/HomePage.tsx (useEffect + readJsonFile + useAppContext pattern)
    - src/components/ui/RefreshButton.tsx (import RefreshButton + isStale + formatter from Task 1)
    - src/components/ui/ListRow.tsx (row component)
    - src/types.ts (TasksSnapshot, TaskItem, ProjectsSnapshot, ProjectItem)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-13 projects layout, D-14 stale/no-data, the Specific Idea about a separate Overdue section above the active list)
  </read_first>
  <action>
    Create `src/components/pages/ProjectsPage.tsx`. Use `useAppContext()` for `app`, `plugin`, and `refreshNonce`. Hold two states with the discriminated union `T | null | 'error'`: `projects: ProjectsSnapshot | null | 'error'` and `tasks: TasksSnapshot | null | 'error'` (clone SocialPage's pattern: empty path → null; readJsonFile returns null → 'error'; otherwise the data).

    Two `useEffect`s, each with `[plugin.settings.projectsSnapshotPath, refreshNonce]` and `[plugin.settings.tasksSnapshotPath, refreshNonce]` respectively (refreshNonce in deps is what makes the page re-read after a successful refresh — NOTION-04). Each reads via `readJsonFile<...>` and sets the union state.

    Layout (NOTION-05, D-13): the page root is `<div className="claudeos-page claudeos-page--projects">`. First child: `<RefreshButton />`. Then a stale banner — if either snapshot is a real object whose `generated_at` `isStale(...)`, render a subtle `<div className="claudeos-stale-label">Stale — last synced {formatted}</div>` (D-14: still render the data below it).

    Projects section: heading "Active Projects". If `projects === null` render the empty-state ("No project data" + "Set the projects snapshot path in Settings, then Refresh."). If `projects === 'error'` render the error empty-state ("Couldn't read projects" + "Check the projects snapshot path."). Otherwise map `projects.items` to `<ListRow>` with `name={p.name}`, `url={p.url}`, and `badges` = the status plus, when `progress` is a finite number, `` `${p.progress}%` ``.

    Tasks section: heading "Tasks". Same null/'error' empty-state handling against the `tasks` union. When data is present: split `tasks.items` into an emphasized group (items whose `due` is overdue OR due-soon) and the remainder. Compute overdue = `due` date strictly before today (YYYY-MM-DD local compare); due-soon = `due` on-or-after today AND on-or-before today + `plugin.settings.dueSoonDays`. Render a sub-section `<h_/div className="claudeos-tasks-overdue">` titled "Overdue & Due Soon" listing the emphasized items as `<ListRow emphasis="overdue" or "due-soon">` (overdue items get `emphasis="overdue"`, due-soon get `emphasis="due-soon"`), each with badges = status plus `due` when present. Only render this sub-section when the emphasized group is non-empty (do not show an empty "Overdue" header — Julian dislikes empty staleness flags). Below it, a sub-section "Active Tasks" listing the remaining items as `<ListRow>` (no emphasis). The Notion deep links come straight from `item.url`.

    All snapshot-derived strings (names, statuses) are rendered as JSX text children only (D-17). Do not construct innerHTML.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectsPage.tsx` reads both `projectsSnapshotPath` and `tasksSnapshotPath` via `readJsonFile`, and both `useEffect` dependency arrays include `refreshNonce`.
    - The component renders `<RefreshButton />` at the top.
    - It uses the `T | null | 'error'` union and renders distinct empty-state copy for null (path empty) vs 'error' (unreadable) — matching the SocialPage convention.
    - Tasks are split into an emphasized "Overdue & Due Soon" group (rendered above) and a remaining "Active Tasks" group; the overdue/due-soon sub-section is omitted when empty.
    - Overdue items pass `emphasis="overdue"` and due-soon items pass `emphasis="due-soon"` to `ListRow`.
    - A stale (>24h) snapshot renders a `claudeos-stale-label` but the data still renders.
    - No `dangerouslySetInnerHTML` in the file.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>ProjectsPage renders projects + tasks with overdue/due-soon emphasis, live-reloads on refreshNonce, and degrades gracefully. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Register Projects in App + Sidebar and add scoped CSS</name>
  <files>src/components/App.tsx, src/components/ui/Sidebar.tsx, styles.css</files>
  <read_first>
    - src/components/App.tsx (PAGES map shape Record<PageId, ComponentType>)
    - src/components/ui/Sidebar.tsx (NAV_ITEMS array shape; NavItem from types.ts)
    - styles.css (existing .claudeos-* tokens: --cos-space-*, --cos-muted, --cos-text; the .claudeos-tile / .claudeos-empty-state patterns; everything is scoped under .claudeos-dashboard)
    - .planning/STATE.md (locked: all CSS scoped under .claudeos-dashboard — D-10 from Phase 1)
  </read_first>
  <action>
    In `src/components/App.tsx`: import `ProjectsPage` from `./pages/ProjectsPage` and add `projects: ProjectsPage` to the `PAGES` record (D-13). The record is typed `Record<PageId, React.ComponentType>`, so adding the key is required now that `PageId` includes `'projects'` — leaving it out is a type error.

    In `src/components/ui/Sidebar.tsx`: add `{ id: 'projects', label: 'Projects', iconId: 'folder-kanban' }` to the `NAV_ITEMS` array (after 'social'). Use a valid Lucide icon id (`folder-kanban` or `kanban` — pick one available in Obsidian's setIcon).

    In `styles.css`: append a new section for Phase 3 components. ALL selectors MUST be nested under `.claudeos-dashboard` (locked decision). Add rules for: `.claudeos-refresh-bar` (flex row, gap), `.claudeos-refresh-btn` and its `--idle/--loading/--success/--error` modifiers (mirror the existing `.claudeos-skill-btn` styling so the two buttons look consistent — reuse the same spacing/radius tokens), the spinner, `.claudeos-list-row` (flex row: name grows, badges + link icon at the end, hover affordance) and its `--overdue` (a left border / accent in a warning color via `var(--text-error)` or `--cos` accent) and `--due-soon` (a softer accent) modifiers, `.claudeos-list-row__name`, `.claudeos-list-row__badges`, `.claudeos-list-row__badge` (pill style), `.claudeos-stale-label` (small, muted), `.claudeos-page--projects` section spacing, and section headings. Reuse existing tokens (`--cos-space-*`, `--cos-muted`, `--cos-text`) — do not hardcode new color hex values where a token exists. Do NOT add any `@import` or external font/network reference (locked: Fontshare import was removed for the same reason).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` `PAGES` includes `projects: ProjectsPage` and imports it.
    - `Sidebar.tsx` `NAV_ITEMS` includes an entry with `id: 'projects'`.
    - `styles.css` contains selectors `.claudeos-refresh-btn`, `.claudeos-list-row`, `.claudeos-stale-label`, and `.claudeos-page--projects`, and every new selector is under the `.claudeos-dashboard` scope.
    - `styles.css` contains no new `@import` statement.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Projects is navigable from the sidebar and styled consistently with existing components, all scoped under .claudeos-dashboard. Build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings → execFile | The node path and script path come from plugin settings (user-controlled config, not runtime user input) and are passed to `execFile`. |
| snapshot JSON → DOM | Task/project names originate in Notion (treated as untrusted) and are rendered in the dashboard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Elevation | RefreshButton execFile | mitigate | Use `execFile(nodePath, [scriptPath])` with an args array — no shell, no string interpolation, no user-typed argument beyond the two configured paths (SEC-03, D-09). |
| T-03-02 | Information Disclosure | snapshot rendering | mitigate | Names rendered as JSX text children only; no `dangerouslySetInnerHTML` (D-17, SEC-01). |
| T-03-03 | Spoofing | Notion deep links | accept | URLs are constructed by the trusted sync script from Notion page ids; ListRow adds `rel="noopener noreferrer"` on the `target="_blank"` anchor. |
</threat_model>

<verification>
- `npm run build` passes (single automated gate — no test runner in repo).
- RefreshButton uses `execFile(nodePath, [scriptPath])` with no shell interpolation.
- ProjectsPage re-reads snapshots when `refreshNonce` changes and renders overdue/due-soon emphasis, no-data, and stale states.
- Projects is reachable from the sidebar.
</verification>

<success_criteria>
- NOTION-04: Refresh runs the script via execFile, shows idle/loading/success/error, displays last-synced HH:mm, and live-reloads pages on success without an Obsidian reload.
- NOTION-05: Projects page renders active projects (status, progress%) and a tasks list emphasizing overdue and due-soon items, each linking to Notion.
- NOTION-08: Projects page degrades to the no-data state when snapshots are missing/unreadable and labels stale (>24h) snapshots while still rendering.
</success_criteria>

<output>
Create `.planning/phases/03-notion-dashboard/03-03-SUMMARY.md` when done. Record: the exact named exports of RefreshButton (isStale, formatter) so Plan 4 imports them, the Lucide icon id chosen for the Projects nav, and any CSS token gaps encountered.
</output>
