---
phase: 03-notion-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/types.ts
  - src/settings/SettingsTab.ts
  - src/context/AppContext.tsx
  - src/views/DashboardView.tsx
autonomous: true
requirements: [NOTION-03, NOTION-04]
requirements_addressed: [NOTION-03, NOTION-04]

must_haves:
  truths:
    - "types.ts exports interfaces matching every snapshot field the sync script emits"
    - "PageId union includes 'projects' and 'newsletter'"
    - "ClaudeOSSettings has the six new Notion paths/values with sensible defaults; settings survive reload via saveData"
    - "AppContext exposes refreshNonce (number) and triggerRefresh (function); existing pages still compile unchanged"
    - "The SettingsTab renders a 'Notion Sync' section with all six fields wired to saveSettings"
    - "npm run build passes with zero TypeScript errors"
  artifacts:
    - path: "src/types.ts"
      provides: "Snapshot interfaces, extended PageId, extended ClaudeOSSettings, updated DEFAULT_SETTINGS"
      contains: "TasksSnapshot"
    - path: "src/context/AppContext.tsx"
      provides: "refreshNonce + triggerRefresh on AppContextType"
      contains: "refreshNonce"
    - path: "src/views/DashboardView.tsx"
      provides: "Provider value supplies live refreshNonce/triggerRefresh via a state-holding wrapper"
      contains: "useState"
    - path: "src/settings/SettingsTab.ts"
      provides: "Notion Sync settings section"
      contains: "Notion Sync"
  key_links:
    - from: "src/views/DashboardView.tsx"
      to: "AppContext.Provider"
      via: "refreshNonce state held in a React wrapper component, passed as Provider value"
      pattern: "triggerRefresh"
    - from: "src/settings/SettingsTab.ts"
      to: "plugin.saveSettings"
      via: "onChange handler per field"
      pattern: "saveSettings"
---

<objective>
Lay the pure-TypeScript foundation the UI plans build on: the snapshot interfaces (matching exactly what Plan 1's script emits), the extended `PageId` union, the six new settings, the `DEFAULT_SETTINGS` update, the "Notion Sync" settings section, and the `refreshNonce`/`triggerRefresh` plumbing on `AppContext`. No UI, no `execFile`, no network code. This unblocks Plans 3 and 4.

Purpose: Give executors of later plans concrete, compile-checked contracts (D-11, D-12) so they never guess at field names or context shape.
Output: Updated `types.ts`, `AppContext.tsx`, `DashboardView.tsx`, `SettingsTab.ts`; `npm run build` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-notion-dashboard/03-CONTEXT.md
@.planning/NOTION-PIPELINE-DESIGN.md
@.planning/phases/03-notion-dashboard/03-01-SUMMARY.md
@src/types.ts
@src/context/AppContext.tsx
@src/views/DashboardView.tsx
@src/settings/SettingsTab.ts
</context>

<artifacts_this_phase_produces>
New / changed symbols introduced by this plan (Phase 3 inventory, Plan 2 of 4):

- `src/types.ts`:
  - `SnapshotMeta` — `{ generated_at: string }`
  - `TaskItem` — `{ name: string; status: string; priority?: string; due?: string; url: string }` (NO `project` — D-06)
  - `TasksSnapshot extends SnapshotMeta` — `{ active_count: number; overdue_count: number; due_soon_count: number; items: TaskItem[] }`
  - `ProjectItem` — `{ name: string; status: string; progress?: number; url: string }` (NO `active_tasks`/`overdue_tasks`)
  - `ProjectsSnapshot extends SnapshotMeta` — `{ active_count: number; items: ProjectItem[] }`
  - `NewsletterItem` — `{ name: string; stage: string; content_type?: string; platform?: string; url: string }`
  - `NewsletterSnapshot extends SnapshotMeta` — `{ by_stage: Record<string, number>; items: NewsletterItem[] }`
  - `PageId` extended to `'home' | 'social' | 'projects' | 'newsletter'`
  - `ClaudeOSSettings` extended with: `tasksSnapshotPath`, `projectsSnapshotPath`, `newsletterSnapshotPath` (string), `syncScriptPath` (string), `nodePath` (string), `dueSoonDays` (number)
  - `DEFAULT_SETTINGS` updated with the new keys
- `src/context/AppContext.tsx`: `AppContextType` gains `refreshNonce: number` and `triggerRefresh: () => void`
- `src/views/DashboardView.tsx`: a React wrapper component holds `refreshNonce` state and supplies the Provider value
- `src/settings/SettingsTab.ts`: a "Notion Sync" heading + six `Setting` rows
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend types.ts with snapshot interfaces, PageId, settings, and defaults</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (current PageId, ClaudeOSSettings, DEFAULT_SETTINGS — extend, do not replace)
    - .planning/phases/03-notion-dashboard/03-01-SUMMARY.md (exact field names the script emitted — match them)
    - .planning/NOTION-PIPELINE-DESIGN.md (§5 data contracts)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-12 settings list and defaults)
  </read_first>
  <action>
    Add these exported interfaces to `src/types.ts`. Field names MUST match what Plan 1's script writes (cross-check against 03-01-SUMMARY.md):

    `SnapshotMeta` with `generated_at: string`.

    `TaskItem` with `name: string`, `status: string`, optional `priority?: string`, optional `due?: string`, `url: string`. Do NOT add a `project` field (D-06). Keep `status`/`priority` as plain `string` (not a literal union) — Notion option names can change and the snapshot already validated them.

    `TasksSnapshot extends SnapshotMeta` with `active_count: number`, `overdue_count: number`, `due_soon_count: number`, `items: TaskItem[]`.

    `ProjectItem` with `name: string`, `status: string`, optional `progress?: number`, `url: string`. Do NOT add `active_tasks` or `overdue_tasks` (Meta formula skipped per critical finding 2).

    `ProjectsSnapshot extends SnapshotMeta` with `active_count: number`, `items: ProjectItem[]`.

    `NewsletterItem` with `name: string`, `stage: string`, optional `content_type?: string`, optional `platform?: string`, `url: string`.

    `NewsletterSnapshot extends SnapshotMeta` with `by_stage: Record<string, number>`, `items: NewsletterItem[]`.

    Extend the `PageId` type alias from `'home' | 'social'` to `'home' | 'social' | 'projects' | 'newsletter'` (D-13).

    Extend the `ClaudeOSSettings` interface — ADD these six keys alongside the existing four (do not remove `lastSyncPath`, `activeProjectsPath`, `linkedinDataPath`, `xDataPath`): `tasksSnapshotPath: string`, `projectsSnapshotPath: string`, `newsletterSnapshotPath: string`, `syncScriptPath: string`, `nodePath: string`, `dueSoonDays: number` (D-12, plus the dual-exec-path critical finding 4: `nodePath` is separate from `syncScriptPath`).

    Update `DEFAULT_SETTINGS` to include the six new keys with these defaults: `tasksSnapshotPath: ".dashboard-data/tasks.json"`, `projectsSnapshotPath: ".dashboard-data/projects.json"`, `newsletterSnapshotPath: ".dashboard-data/newsletter.json"`, `syncScriptPath: ""` (D-09 — user must set once), `nodePath: "node"` (critical finding 4 — defaults to PATH lookup; user overrides with the full nvm path on this machine), `dueSoonDays: 3` (D-05 confirmed).

    Keep all existing Phase 2 types (`TileSyncData`, `TileCountData`, `LinkedInData`, `XData`, `NavItem`) untouched.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/types.ts` exports `SnapshotMeta`, `TaskItem`, `TasksSnapshot`, `ProjectItem`, `ProjectsSnapshot`, `NewsletterItem`, `NewsletterSnapshot`.
    - `TaskItem` has no `project` member; `ProjectItem` has no `active_tasks`/`overdue_tasks` members.
    - `PageId` equals `'home' | 'social' | 'projects' | 'newsletter'`.
    - `ClaudeOSSettings` contains all ten keys (four original + six new); `nodePath` and `syncScriptPath` are distinct string keys.
    - `DEFAULT_SETTINGS` sets `nodePath: "node"`, `syncScriptPath: ""`, `dueSoonDays: 3`, and the three snapshot paths default under `.dashboard-data/`.
    - `npm run build` exits 0 (tsc --noEmit + esbuild production both succeed).
  </acceptance_criteria>
  <done>All snapshot interfaces, the extended PageId, the six settings, and updated defaults compile cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add refreshNonce + triggerRefresh to AppContext and supply them from a state wrapper in DashboardView</name>
  <files>src/context/AppContext.tsx, src/views/DashboardView.tsx</files>
  <read_first>
    - src/context/AppContext.tsx (current AppContextType + useAppContext)
    - src/views/DashboardView.tsx (the Provider value is set in onOpen, a class method — it cannot hold React state directly)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-10, D-11 — nonce semantics)
  </read_first>
  <action>
    In `src/context/AppContext.tsx`, extend `AppContextType` with two new fields: `refreshNonce: number` and `triggerRefresh: () => void` (D-11). Leave `app` and `plugin` as-is. Leave `useAppContext()` unchanged.

    In `src/views/DashboardView.tsx`, the Provider value is currently a static object literal `{ app, plugin }` inside the class method `onOpen` — a class method cannot call `useState`. Introduce a small React function component (e.g. `DashboardRoot`) defined in this file that: takes `app` and `plugin` as props; calls `const [refreshNonce, setRefreshNonce] = useState(0)`; defines `const triggerRefresh = useCallback(() => setRefreshNonce(n => n + 1), [])`; and renders `<AppContext.Provider value={{ app, plugin, refreshNonce, triggerRefresh }}><DashApp /></AppContext.Provider>`. In `onOpen`, render `<StrictMode><DashboardRoot app={this.app} plugin={this.plugin} /></StrictMode>` instead of the inline Provider. Import `useState` and `useCallback` from `react`.

    Do NOT change `renderSafeHTML` or the `onClose` unmount logic. Do NOT change the mount target (`this.contentEl`) — that is a locked decision.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `AppContextType` declares `refreshNonce: number` and `triggerRefresh: () => void`.
    - `DashboardView.tsx` imports `useState` and `useCallback` and defines a function component that holds `refreshNonce` state.
    - The Provider `value` object includes `refreshNonce` and `triggerRefresh` (so `useAppContext()` returns them).
    - The mount still uses `this.contentEl`; `onClose` still calls `this.root?.unmount()`.
    - `npm run build` exits 0. (Existing HomePage/SocialPage still compile because they read only `app`/`plugin`.)
  </acceptance_criteria>
  <done>AppContext carries a live, incrementable refreshNonce supplied by a React state wrapper; the build is green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add a Notion Sync section to SettingsTab with all six fields</name>
  <files>src/settings/SettingsTab.ts</files>
  <read_first>
    - src/settings/SettingsTab.ts (existing Setting/onChange/saveSettings pattern to clone)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-12)
    - src/types.ts (the six new setting keys and defaults — must match exactly)
  </read_first>
  <action>
    In `src/settings/SettingsTab.ts`, after the existing "Data File Paths" settings, add a new section. Start it with `new Setting(containerEl).setName('Notion Sync').setHeading();` then add six `Setting` rows, each cloning the existing addText pattern (read `this.plugin.settings.<key>`, write it in `onChange`, then `await this.plugin.saveSettings()`):

    1. Name "Node executable path", desc "Path to node.exe used to run the sync script. Use the full path if node is not on Obsidian's PATH (e.g. C:\\Users\\scull\\AppData\\Local\\nvm\\v20.20.2\\node.exe). Default: node", bound to `nodePath`.
    2. Name "Sync script path", desc "Absolute path to scripts/notion-sync.mjs. Required — set this once.", bound to `syncScriptPath`.
    3. Name "Tasks snapshot file", desc "Path to tasks.json written by the sync script.", bound to `tasksSnapshotPath`.
    4. Name "Projects snapshot file", desc "Path to projects.json written by the sync script.", bound to `projectsSnapshotPath`.
    5. Name "Newsletter snapshot file", desc "Path to newsletter.json written by the sync script.", bound to `newsletterSnapshotPath`.
    6. Due-soon window — use `addText` but parse to an integer: read `String(this.plugin.settings.dueSoonDays)`; in `onChange`, `const n = parseInt(value, 10); this.plugin.settings.dueSoonDays = Number.isFinite(n) && n >= 0 ? n : 3;` then `await this.plugin.saveSettings()`. Name "Due-soon window (days)", desc "Tasks due within this many days are emphasized. Default: 3".

    Do NOT remove the existing four Phase 2 settings rows. Keep the bound key names identical to the `ClaudeOSSettings` keys.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `SettingsTab.ts` contains a `setName('Notion Sync').setHeading()` call.
    - There are six new `Setting` rows binding to `nodePath`, `syncScriptPath`, `tasksSnapshotPath`, `projectsSnapshotPath`, `newsletterSnapshotPath`, and `dueSoonDays` respectively.
    - The `dueSoonDays` row parses input with `parseInt` and falls back to `3` on invalid input.
    - Every new row calls `await this.plugin.saveSettings()` in its `onChange`.
    - The existing four Phase 2 rows are unchanged.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>A Notion Sync settings section persists all six values via saveData; build is green.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero TypeScript errors (the single automated gate for this pure-TS plan; the repo has no test runner — tsc --noEmit is the contract checker).
- Snapshot interface field names match the script output recorded in 03-01-SUMMARY.md.
- Existing Home and Social pages compile unchanged (backward-compatible context extension).
</verification>

<success_criteria>
- NOTION-03 (type half): snapshot files now have TypeScript interfaces that the plugin can consume with full type safety.
- NOTION-04 (plumbing half): `refreshNonce`/`triggerRefresh` exist on AppContext so the RefreshButton (Plan 3) can trigger re-reads; settings carry the dual exec paths (`nodePath` + `syncScriptPath`) the RefreshButton needs.
</success_criteria>

<output>
Create `.planning/phases/03-notion-dashboard/03-02-SUMMARY.md` when done. Record: any field-name reconciliation between types.ts and the actual snapshot output, and the exact name/signature of the DashboardRoot wrapper component for Plan 3/4 reference.
</output>
