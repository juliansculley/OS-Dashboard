---
phase: 03-notion-dashboard
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/components/pages/NewsletterPage.tsx
  - src/components/App.tsx
  - src/components/ui/Sidebar.tsx
  - styles.css
  - scripts/schedule-notion-sync.ps1
autonomous: false
requirements: [NOTION-06, NOTION-07, NOTION-08]
requirements_addressed: [NOTION-06, NOTION-07, NOTION-08]

must_haves:
  truths:
    - "Newsletter page shows stage counts for only the stages that have items (no zero-count stages)"
    - "Newsletter page lists items (name, stage, content type) each linking to its Notion page"
    - "Newsletter page re-reads its snapshot on refreshNonce and degrades to no-data / shows a stale label like the Projects page"
    - "Newsletter appears in the sidebar nav and routes to the Newsletter page"
    - "Running schedule-notion-sync.ps1 registers a Windows Task Scheduler job that runs the same notion-sync.mjs daily at 06:00 using the same code path as the Refresh button"
  artifacts:
    - path: "src/components/pages/NewsletterPage.tsx"
      provides: "Stage counts + item list with no-data/stale states"
      contains: "refreshNonce"
    - path: "scripts/schedule-notion-sync.ps1"
      provides: "Windows Task Scheduler registration for daily background sync"
      contains: "Register-ScheduledTask"
    - path: "src/components/App.tsx"
      provides: "PAGES map includes newsletter"
      contains: "NewsletterPage"
  key_links:
    - from: "src/components/pages/NewsletterPage.tsx"
      to: "readJsonFile"
      via: "useEffect with refreshNonce in deps reads newsletterSnapshotPath"
      pattern: "readJsonFile"
    - from: "scripts/schedule-notion-sync.ps1"
      to: "scripts/notion-sync.mjs"
      via: "scheduled action runs node notion-sync.mjs (same entry point as Refresh)"
      pattern: "notion-sync.mjs"
---

<objective>
Complete the phase: build the Newsletter page (stage counts + item list, with the same no-data/stale/live-reload behavior as Projects) and add the Windows Task Scheduler registration script so the same `notion-sync.mjs` runs on a background cadence. After this plan, both new pages are navigable, refresh-aware, and the data can stay fresh without opening Obsidian.

Purpose: Deliver NOTION-06 (Newsletter page), NOTION-07 (scheduled background sync via the same code path), and complete NOTION-08 across all new pages.
Output: `NewsletterPage.tsx`, wired into `App.tsx`/`Sidebar.tsx`, scoped CSS, and `scripts/schedule-notion-sync.ps1`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-notion-dashboard/03-CONTEXT.md
@.planning/phases/03-notion-dashboard/03-02-SUMMARY.md
@.planning/phases/03-notion-dashboard/03-03-SUMMARY.md
@src/components/pages/ProjectsPage.tsx
@src/components/ui/RefreshButton.tsx
@src/components/ui/ListRow.tsx
@src/components/App.tsx
@src/components/ui/Sidebar.tsx
@src/types.ts
@scripts/notion-sync.mjs
@styles.css
</context>

<artifacts_this_phase_produces>
New symbols (Phase 3 inventory, Plan 4 of 4 — final):

- `src/components/pages/NewsletterPage.tsx` — `NewsletterPage` component. Reads the newsletter snapshot, renders RefreshButton, a stage-count list (only non-zero stages), and an item list; handles no-data and stale states.
- `src/components/App.tsx`: `PAGES` map gains `newsletter: NewsletterPage`.
- `src/components/ui/Sidebar.tsx`: `NAV_ITEMS` gains a Newsletter entry.
- `styles.css`: scoped rules for `.claudeos-page--newsletter` and the stage-count list, under `.claudeos-dashboard`.
- `scripts/schedule-notion-sync.ps1` — PowerShell helper that registers a daily Windows Task Scheduler job running `notion-sync.mjs` at 06:00, parameterized by node path and script path, with usage docs in the header.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build NewsletterPage with stage counts, item list, no-data/stale states</name>
  <files>src/components/pages/NewsletterPage.tsx</files>
  <read_first>
    - src/components/pages/ProjectsPage.tsx (clone its structure: union state, refreshNonce useEffect deps, RefreshButton at top, stale banner, empty-state copy)
    - src/components/ui/RefreshButton.tsx (import RefreshButton + isStale + the HH:mm formatter named exports from Plan 3 — confirm exact names in 03-03-SUMMARY.md)
    - src/components/ui/ListRow.tsx (row component + props)
    - src/types.ts (NewsletterSnapshot, NewsletterItem — by_stage is Record<string, number>)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-13 newsletter layout, D-14 stale/no-data, Specific Idea: show only stages with items)
  </read_first>
  <action>
    Create `src/components/pages/NewsletterPage.tsx` mirroring ProjectsPage. Use `useAppContext()` for `app`, `plugin`, `refreshNonce`. Hold one union state `newsletter: NewsletterSnapshot | null | 'error'` (empty path → null; readJsonFile null → 'error'; else data). One `useEffect` with deps `[plugin.settings.newsletterSnapshotPath, refreshNonce]` reading `readJsonFile<NewsletterSnapshot>`.

    Layout (NOTION-06, D-13): root `<div className="claudeos-page claudeos-page--newsletter">`. First child `<RefreshButton />`. If the snapshot is a real object whose `generated_at` `isStale(...)`, render the `claudeos-stale-label` banner (still render data below — D-14).

    No-data handling: if `newsletter === null` render the empty-state ("No newsletter data" + "Set the newsletter snapshot path in Settings, then Refresh."); if `=== 'error'` render the error empty-state ("Couldn't read newsletter data" + "Check the newsletter snapshot path."). Use the same `claudeos-empty-state` markup ProjectsPage uses.

    Stage counts (Specific Idea — only stages with items): heading "Pipeline". Take `newsletter.by_stage` (a `Record<string,number>`), get its entries, FILTER OUT any entry whose count is 0, sort by the stage key ascending (stage names are prefixed with a number like "3. Single outline", so a plain string sort orders them sensibly), and render each as a row `<div className="claudeos-stage-count"><span class name>{stage}</span><span class count>{count}</span></div>`. Do not render zero-count stages.

    Item list: heading "Items". Map `newsletter.items` to `<ListRow>` with `name={i.name}`, `url={i.url}`, and `badges` = `[i.stage]` plus, when present, `i.content_type` (and optionally `i.platform`). No emphasis variant needed here (default `'none'`).

    All snapshot strings rendered as JSX text children only (D-17). No `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `NewsletterPage.tsx` reads `newsletterSnapshotPath` via `readJsonFile` with `refreshNonce` in the `useEffect` deps.
    - It renders `<RefreshButton />` at the top and imports `isStale` from `RefreshButton`.
    - Stage-count rendering filters out entries whose count is 0 (zero-count stages are not rendered).
    - Items render via `ListRow` with badges including the stage and content type.
    - null vs 'error' produce distinct empty-state copy (same convention as Projects/Social).
    - A stale (>24h) snapshot renders the stale label but data still renders.
    - No `dangerouslySetInnerHTML` in the file.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>NewsletterPage renders non-zero stage counts + linked items, live-reloads on refresh, and degrades gracefully. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register Newsletter in App + Sidebar and add scoped CSS</name>
  <files>src/components/App.tsx, src/components/ui/Sidebar.tsx, styles.css</files>
  <read_first>
    - src/components/App.tsx (PAGES map — projects already added in Plan 3; add newsletter)
    - src/components/ui/Sidebar.tsx (NAV_ITEMS — Projects already added in Plan 3; add Newsletter)
    - styles.css (Plan 3 added refresh/list-row/projects rules under .claudeos-dashboard; follow the same scoping and tokens)
    - .planning/STATE.md (locked: all CSS scoped under .claudeos-dashboard)
  </read_first>
  <action>
    In `src/components/App.tsx`: import `NewsletterPage` from `./pages/NewsletterPage` and add `newsletter: NewsletterPage` to the `PAGES` record (D-13). With `PageId` now including `'newsletter'`, the `Record<PageId, React.ComponentType>` type requires this key — omitting it is a compile error.

    In `src/components/ui/Sidebar.tsx`: add `{ id: 'newsletter', label: 'Newsletter', iconId: 'newspaper' }` to `NAV_ITEMS` (after the Projects entry). Use a valid Lucide icon id available in Obsidian's setIcon (`newspaper` or `mail`).

    In `styles.css`: append rules under `.claudeos-dashboard` for `.claudeos-page--newsletter` section spacing and `.claudeos-stage-count` (flex row: stage name grows, count right-aligned, pill or muted count). Reuse existing tokens (`--cos-space-*`, `--cos-muted`, `--cos-text`) and the list-row/empty-state styling from Plan 3. Do NOT add any `@import` or external network reference.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` `PAGES` includes `newsletter: NewsletterPage` and imports it.
    - `Sidebar.tsx` `NAV_ITEMS` includes an entry with `id: 'newsletter'`.
    - `styles.css` contains `.claudeos-page--newsletter` and `.claudeos-stage-count` selectors, both under the `.claudeos-dashboard` scope.
    - `styles.css` contains no new `@import` statement.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Newsletter is navigable from the sidebar and styled consistently. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create scripts/schedule-notion-sync.ps1 (Task Scheduler registration)</name>
  <files>scripts/schedule-notion-sync.ps1</files>
  <read_first>
    - scripts/notion-sync.mjs (the entry point the scheduled job must run — same code path as the Refresh button, NOTION-07)
    - .planning/phases/03-notion-dashboard/03-CONTEXT.md (D-15 — Task Scheduler, daily 06:00 local)
  </read_first>
  <action>
    Create `scripts/schedule-notion-sync.ps1`. Begin with a comment-based help / header block documenting usage: how to run it from an elevated PowerShell, what it does (registers a daily Task Scheduler job at 06:00 local that runs the Notion sync), and the two parameters. Define `param([string]$NodePath = "C:\Users\scull\AppData\Local\nvm\v20.20.2\node.exe", [string]$ScriptPath, [string]$TaskName = "ClaudeOS Notion Sync")`. If `$ScriptPath` is not provided, default it to the `notion-sync.mjs` sitting next to this script: resolve `$PSScriptRoot\notion-sync.mjs` and use it if it exists; otherwise write an error and exit.

    Build the action with `New-ScheduledTaskAction -Execute $NodePath -Argument "`"$ScriptPath`"" ` (quote the script path to survive spaces) and a working directory of the script's parent. Build the trigger with `New-ScheduledTaskTrigger -Daily -At 6:00am` (06:00 local, D-15). Register with `Register-ScheduledTask -TaskName $TaskName -Action $action -Trigger $trigger -Description "Runs the ClaudeOS Notion sync daily at 06:00" -Force` so re-running updates the existing task rather than failing. After registration, print a confirmation line with the task name and next run time, and a note that the NOTION_TOKEN environment variable (or %USERPROFILE%\.claudeos\notion.env) must be available to the user the task runs as. Because the same `notion-sync.mjs` is invoked, this is the identical code path as the manual Refresh button (NOTION-07).

    Do NOT embed any token in the script. Do NOT pass any secret as an argument.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/schedule-notion-sync.ps1','utf8'); const need=['Register-ScheduledTask','New-ScheduledTaskTrigger','notion-sync.mjs','-Daily','param(']; const missing=need.filter(t=>!s.includes(t)); if(missing.length){console.error('MISSING:',missing.join(', '));process.exit(1);} if(/NOTION_TOKEN\s*=\s*[\w-]/.test(s)){console.error('script must not embed a token value');process.exit(1);} console.log('ps1 source assertions OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/schedule-notion-sync.ps1` exists and contains `param(`, `Register-ScheduledTask`, `New-ScheduledTaskTrigger`, `-Daily`, and references `notion-sync.mjs`.
    - It accepts `NodePath`, `ScriptPath`, and `TaskName` parameters; `NodePath` defaults to the machine's nvm node path.
    - It uses `-Force` on `Register-ScheduledTask` so re-running is idempotent.
    - The script embeds no token value (no `NOTION_TOKEN=<secret>` literal).
    - The `node -e` gate prints `ps1 source assertions OK` and exits 0.
  </acceptance_criteria>
  <done>The PowerShell script registers a daily 06:00 Task Scheduler job invoking the same sync script, with no embedded secrets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify pages render in Obsidian and the scheduled job registers</name>
  <what-built>
    The Newsletter page (stage counts + linked items, no-data/stale states), wired into the sidebar, plus a PowerShell script that registers a daily Windows Task Scheduler job running the same sync script. Combined with Plan 3, both Notion pages are now navigable and refresh-aware.
  </what-built>
  <how-to-verify>
    1. Reload the plugin in Obsidian (or it hot-reloads if `npm run dev` is running). Open the ClaudeOS Dashboard.
    2. Confirm the sidebar now shows Projects and Newsletter entries. Click each.
    3. With valid snapshots present (from Plan 1's live run), confirm: the Projects page shows active projects with progress and an "Overdue & Due Soon" section above the active tasks; the Newsletter page shows only non-zero pipeline stages and an item list. Click a row and confirm it opens the correct Notion page in your browser.
    4. Click Refresh on either page. Confirm it transitions loading → success and the "Last synced HH:mm" updates without reloading Obsidian.
    5. Temporarily clear (or point to a missing path) one snapshot setting and confirm the page shows the no-data state, then restore it.
    6. From an elevated PowerShell, run:
       & "C:\Users\scull\AppData\Local\nvm\v20.20.2\node.exe" --version   (confirm 18+)
       powershell -ExecutionPolicy Bypass -File scripts\schedule-notion-sync.ps1 -ScriptPath (Resolve-Path scripts\notion-sync.mjs)
       Then confirm in Task Scheduler that a task named "ClaudeOS Notion Sync" exists with a daily 06:00 trigger. (Optional) Right-click → Run, and confirm the snapshots get rewritten.
  </how-to-verify>
  <resume-signal>Type "approved" once both pages render with live data, links open Notion, Refresh live-reloads, no-data degrades correctly, and the scheduled task is registered. Otherwise describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot JSON → DOM | Newsletter titles/stages come from Notion (untrusted) and render in the dashboard. |
| PowerShell script → OS scheduler | The script registers a task that runs node against a configured script path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | NewsletterPage rendering | mitigate | Titles/stages rendered as JSX text children; no `dangerouslySetInnerHTML` (D-17, SEC-01). |
| T-04-02 | Tampering | schedule-notion-sync.ps1 | mitigate | Script embeds no secrets; token stays in the env var / non-synced file the task user reads at runtime (NOTION-02). Script path is quoted to avoid argument injection from spaces. |
| T-04-03 | Elevation | scheduled task runs node | accept | Task runs the same committed `notion-sync.mjs` the user already runs manually; no new privilege beyond the user's own context. |
</threat_model>

<verification>
- `npm run build` passes (single automated gate — no test runner in repo).
- NewsletterPage re-reads on refreshNonce, filters zero-count stages, degrades to no-data, labels stale snapshots.
- schedule-notion-sync.ps1 registers a daily 06:00 job invoking the same notion-sync.mjs entry point with no embedded secrets.
- Human checkpoint confirms both pages render in Obsidian, links open Notion, Refresh live-reloads, and the scheduled task exists.
</verification>

<success_criteria>
- NOTION-06: Newsletter page renders pipeline counts by stage (non-zero only) and an item list (name, stage, content type) each linking to Notion.
- NOTION-07: the sync script runs on a Windows Task Scheduler cadence (daily 06:00) using the same code path as the manual Refresh button.
- NOTION-08: the Newsletter page (and, with Plan 3, the Projects page) degrades to the no-data state when its snapshot is missing/unreadable and labels stale (>24h) snapshots while still rendering.
</success_criteria>

<output>
Create `.planning/phases/03-notion-dashboard/03-04-SUMMARY.md` when done. Record: the Lucide icon id chosen for Newsletter, the registered Task Scheduler task name, and any Obsidian render issues found during the human checkpoint.
</output>
